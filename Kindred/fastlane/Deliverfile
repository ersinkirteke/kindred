# App Store Connect delivery configuration
app_identifier "com.ersinkirteke.kindred"
team_id "CV9G42QVG4"

# Submission configuration
force true
skip_binary_upload false

# App details
platform "ios"
primary_category "FOOD_AND_DRINK"
copyright "2026 Ersin Kirteke"

# Pricing: set manually in ASC (free app). price_tier removed because
# fastlane 2.232.2's pricing API is incompatible with current ASC API.

# App Review Information
app_review_information({
  first_name: "Ersin",
  last_name: "Kirteke",
  phone_number: "+905555555555",
  email_address: "support@kindred.app",
  demo_user: ENV["BETA_DEMO_EMAIL"] || "beta@kindred.app",
  demo_password: ENV["BETA_DEMO_PASSWORD"] || "demo123",
  notes: "This app uses ElevenLabs AI voice cloning technology. Voice profiles require explicit user consent before upload. Users can delete voice profiles from Settings at any time. Demo account provided above."
})

# Age Rating: set manually in ASC. rating_config.json triggers a fastlane 2.232.2
# bug where ASC API expects booleans for new fields but fastlane sends strings.
# app_rating_config_path "./fastlane/rating_config.json"

# Release configuration
automatic_release true
phased_release false
submission_information({
  add_id_info_uses_idfa: false,
  content_rights_has_rights: true,
  content_rights_contains_third_party_content: true,
  export_compliance_uses_encryption: true,
  export_compliance_encryption_updated: false,
  export_compliance_is_exempt: true
})
