---
phase: 05-guest-browsing-feed
task: checkpoint-verification
total_tasks: 4 plans (3 waves)
status: in_progress
last_updated: 2026-03-01T20:57:56.996Z
---

<current_state>
Phase 5 execution is nearly complete. All 4 plans (05-01 through 05-04) have been executed by GSD executor agents. We are at the **checkpoint verification** step of plan 05-04 (Wave 3). The user requested a build to verify in iOS Simulator. Build fixes are in progress — committed one round of fixes (c62092e) but the build is not yet clean. Need to continue fixing remaining compilation errors, then install on simulator for the user's checkpoint verification.

After checkpoint passes: run the GSD verifier, then update roadmap.
</current_state>

<completed_work>

## Plans Executed
- **05-01** (Wave 1): TCA dependencies, domain models, guest session, location, network clients — DONE, SUMMARY created, 3 commits
- **05-02** (Wave 2): Swipeable card stack with FeedReducer, RecipeCardView, SwipeCardStack, action buttons — DONE, SUMMARY created, 3 commits
- **05-03** (Wave 2): Recipe detail screen with parallax hero, ingredients, steps, sticky bar — DONE, SUMMARY created, 3 commits
- **05-04** (Wave 3): Location picker, city search, navigation wiring, bookmark badge — Tasks 1-2 DONE (2 commits), Task 3 is checkpoint (human-verify)

## Build Fixes Applied (commit c62092e)
- Kingfisher version 7.x → 8.x to match root project
- Removed @DependencyClient macro (not in dependency chain, used manual DependencyKey instead)
- Apollo 2.x API migration: .returnCacheDataAndFetch → .cacheFirst, .fetchIgnoringCacheData → .networkOnly
- Fixed GraphQLNullable wrapping for RecipesQuery params
- Removed duplicate ViralBadge (was in both ParallaxHeader.swift and ViralBadge.swift)
- Removed iOS 18-only matchedTransitionSource/navigationTransition
- Fixed GraphQLEnum<DifficultyLevel> → String conversion with .rawValue
- Fixed KindredButton API mismatches (no icon:/size: params — replaced with raw Button)
- Fixed CardSurface API (no padding: param)
- Excluded Schema/Package.swift from NetworkClient target
- Fixed @MainActor isolation on GuestSessionStore.getGuestUserId (nonisolated)
- Fixed non-optional arrays (ingredients/steps) with unnecessary optional chaining
</completed_work>

<remaining_work>

## Immediate: Fix Remaining Build Errors
Last build had 1 error remaining:
- `RecipeCardView.swift:14` — `CardSurface(padding: 0)` → fixed to `CardSurface {` but need to rebuild to check for more errors
- There may be additional errors revealed after this one is resolved (HapticFeedback, shimmer(), ErrorStateView, etc. — APIs that may not exist in DesignSystem)
- Run: `xcodebuild -project Kindred/Kindred.xcodeproj -scheme Kindred -destination 'platform=iOS Simulator,id=CB29AD6F-B7B2-4FA6-A567-A41C27059D54' -derivedDataPath /tmp/KindredBuild -skipMacroValidation build 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | sort -u`

## After Build Succeeds
1. Install on simulator: `xcrun simctl install CB29AD6F-B7B2-4FA6-A567-A41C27059D54 /tmp/KindredBuild/Build/Products/Debug-iphonesimulator/Kindred.app && xcrun simctl launch CB29AD6F-B7B2-4FA6-A567-A41C27059D54 [bundle-id]`
2. Present checkpoint to user for verification of complete feed flow
3. User approves or reports issues
4. Spawn continuation agent for 05-04 Task 3 with user response

## After Checkpoint
1. Spot-check 05-04 SUMMARY.md (agent needs to create it after checkpoint)
2. Run GSD verifier: `Task(subagent_type="gsd-verifier", model="sonnet", ...)`
3. Phase completion: `node gsd-tools.cjs phase complete "05"`
4. Commit docs and offer next step
</remaining_work>

<decisions_made>

- Apollo 2.x cache policy mapping: .returnCacheDataAndFetch → .cacheFirst (single response, cache-first), .fetchIgnoringCacheData → .networkOnly
- Removed @DependencyClient macro rather than adding DependenciesMacros dependency — code already had manual DependencyKey conformance
- Removed iOS 18 hero transition (matchedTransitionSource, navigationTransition .zoom) since target is iOS 17
- Replaced KindredButton icon buttons with raw SwiftUI Button + Image since KindredButton only supports text titles
- Used nonisolated for getGuestUserId() since it only accesses UserDefaults (thread-safe)
- Excluded NetworkClient/Sources/Schema/Package.swift — Apollo-generated sub-package incorrectly picked up as source
</decisions_made>

<blockers>
- Build not yet clean — need to iterate through remaining compilation errors
- The `-skipMacroValidation` flag is needed for xcodebuild (TCA/swift-dependencies macros)
- iPhone 16 simulator already booted (CB29AD6F-B7B2-4FA6-A567-A41C27059D54)
</blockers>

<context>
The execution agents wrote code against Apollo iOS 1.x API patterns and some non-existent DesignSystem APIs. The root project uses Apollo 2.x which has a completely different fetch API (GraphQLResponse instead of GraphQLResult, CachePolicy.Query.SingleResponse enum, GraphQLNullable for optional params). The fix approach is iterative: build, find errors, fix, rebuild. Each cycle typically reveals 1-3 new errors as the compiler gets further.

Key files to watch for remaining errors: FeedView.swift (shimmer(), ErrorStateView references), RecipeCardView.swift, any file using Apollo fetch. The DesignSystem package has: KindredButton (text only, no icon), CardSurface (hasShadow only), color tokens, typography, spacing.
</context>

<next_action>
1. Run the build command above to check if CardSurface fix resolved last error
2. Fix any remaining errors (likely HapticFeedback utility, shimmer modifier, ErrorStateView)
3. Once BUILD SUCCEEDED, install on simulator and present checkpoint to user
4. After user approval, spawn 05-04 continuation agent, then run verifier
</next_action>
