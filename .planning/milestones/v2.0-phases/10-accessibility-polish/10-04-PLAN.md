---
phase: 10-accessibility-polish
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailReducer.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift
autonomous: true
requirements:
  - ACCS-05

must_haves:
  truths:
    - "All text scales correctly with Dynamic Type at AX1-AX5 sizes without layout breaking"
    - "RecipeCardView grows to fit content at AX sizes (no fixed 400pt height) and becomes scrollable if content overflows"
    - "Action buttons (Skip/Listen/Bookmark) grow with @ScaledMetric at AX sizes"
    - "All screens use ErrorStateView for error states and EmptyStateView for empty states consistently"
    - "Offline actions show toast notification with 'This requires internet connection'"
    - "Recipe detail disables Listen when offline with message 'Voice narration requires internet'"
  artifacts:
    - path: "Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift"
      provides: "@ScaledMetric typography, flexible card height, scaled action buttons"
    - path: "Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift"
      provides: "Consistent ErrorStateView and EmptyStateView usage"
    - path: "Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift"
      provides: "@ScaledMetric typography, offline listen button disable"
  key_links:
    - from: "All view files"
      to: "DesignSystem/Typography.swift"
      via: "@ScaledMetric properties with kindred*Scaled(size:) methods"
      pattern: "@ScaledMetric.*kindred"
---

<objective>
Dynamic Type layout adaptations and error/empty state consistency across all features. Every screen adopts @ScaledMetric typography from Plan 01, adapts layouts for accessibility sizes, and uses consistent error/empty states with offline handling.

Purpose: Ensures the app is fully usable at accessibility text sizes (AX1-AX5) and provides consistent error/empty/offline states across every screen.

Output: Updated view files with @ScaledMetric typography, flexible layouts, consistent error handling, and offline states.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-accessibility-polish/10-CONTEXT.md
@.planning/phases/10-accessibility-polish/10-RESEARCH.md

# Need Plan 01 SUMMARY for @ScaledMetric pattern and new components
@.planning/phases/10-accessibility-polish/10-01-SUMMARY.md
# Need Plan 02 SUMMARY for connectivity state
@.planning/phases/10-accessibility-polish/10-02-SUMMARY.md

@Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift
@Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift
@Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift
@Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift
@Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailReducer.swift
@Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift
@Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
@Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift
@Kindred/Packages/DesignSystem/Sources/DesignSystem/Components/ErrorStateView.swift
@Kindred/Packages/DesignSystem/Sources/DesignSystem/Components/EmptyStateView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt @ScaledMetric typography and flexible layouts in Feed and RecipeDetail views</name>
  <files>Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift, Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift, Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift, Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift</files>
  <action>
**RecipeCardView.swift:**

1. Add @ScaledMetric properties for all typography sizes used in the card:
   ```swift
   @ScaledMetric(relativeTo: .title2) private var heading2Size: CGFloat = 22
   @ScaledMetric(relativeTo: .headline) private var bodySize: CGFloat = 18
   @ScaledMetric(relativeTo: .caption) private var captionSize: CGFloat = 14
   @ScaledMetric(relativeTo: .caption) private var iconSize: CGFloat = 14
   @ScaledMetric(relativeTo: .headline) private var buttonSize: CGFloat = 56
   ```

2. Replace `.font(.kindredHeading2())` with `.font(.kindredHeading2Scaled(size: heading2Size))` (and similarly for all other font usages in this view).

3. Replace hardcoded icon sizes (clock, flame, heart) with `iconSize` scaled metric.

4. Per locked decision: "RecipeCardView: remove fixed 400pt height, card grows to fit content. Card becomes scrollable if content overflows at AX sizes." Remove any `.frame(height: 400)` or similar fixed-height constraint. Wrap card content in `ScrollView` when `dynamicTypeSize.isAccessibilitySize` to handle overflow.

5. Per locked decision: "Action buttons (Skip/Listen/Bookmark): grow with @ScaledMetric proportionally at AX sizes." Use `buttonSize` for action button dimensions instead of hardcoded 56.

6. Metadata icons: use `@ScaledMetric` `iconSize` for clock, flame, heart icons per locked decision "Icons scale with text via @ScaledMetric."

**FeedView.swift:**

1. Add @ScaledMetric properties for heading text sizes used in feed header.
2. Ensure EmptyStateView is used when no recipes and no error (empty state). Ensure ErrorStateView is used when fetch fails with retry button.
3. Per locked decision: "All error states use ErrorStateView consistently" — check existing error handling and migrate to ErrorStateView if using ad-hoc error text.

**RecipeDetailView.swift:**

1. Add @ScaledMetric properties for all typography levels:
   ```swift
   @ScaledMetric(relativeTo: .largeTitle) private var titleSize: CGFloat = 34
   @ScaledMetric(relativeTo: .headline) private var bodySize: CGFloat = 18
   @ScaledMetric(relativeTo: .caption) private var captionSize: CGFloat = 14
   ```

2. Replace `.font(.kindredLargeTitle())` etc. with scaled variants using the @ScaledMetric properties.

3. Per locked decision: "Voice narration: disable Listen button when offline with message 'Voice narration requires internet'." Add offline check: if the app is offline (from parent state or own state), disable the Listen/play button and show "Voice narration requires internet" hint text.

4. Per locked decision: "Recipe detail works offline via Apollo's SQLite cache (cacheFirst policy). If not cached: 'Available when online' placeholder." When offline and no cached data, show `EmptyStateView(icon: "wifi.slash", title: "Available when online", message: "This recipe will be available when you're connected")`.

**ExpandedPlayerView.swift:**

1. Add @ScaledMetric properties for player typography:
   ```swift
   @ScaledMetric(relativeTo: .title2) private var titleSize: CGFloat = 22
   @ScaledMetric(relativeTo: .headline) private var bodySize: CGFloat = 18
   @ScaledMetric(relativeTo: .caption) private var captionSize: CGFloat = 14
   @ScaledMetric(relativeTo: .title) private var playButtonSize: CGFloat = 64
   ```

2. Replace hardcoded font sizes and play button size with @ScaledMetric values.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && grep -c "@ScaledMetric" Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift && grep -c "@ScaledMetric" Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift && grep -c "@ScaledMetric" Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift</automated>
  </verify>
  <done>
- RecipeCardView: @ScaledMetric for all typography and icons, no fixed 400pt height, scrollable at AX, scaled action buttons
- FeedView: Consistent ErrorStateView and EmptyStateView usage
- RecipeDetailView: @ScaledMetric typography, offline listen disable, offline empty state
- ExpandedPlayerView: @ScaledMetric for all sizes including 64dp play button
  </done>
</task>

<task type="auto">
  <name>Task 2: Error/empty state consistency and offline handling across Profile, Paywall, and Onboarding</name>
  <files>Kindred/Packages/ProfileFeature/Sources/ProfileView.swift, Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift, Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift, Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift, Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailReducer.swift</files>
  <action>
**ProfileView.swift:**

1. Add @ScaledMetric for typography sizes used in profile.
2. Ensure ErrorStateView used for any error states (e.g., failed to load profile data).
3. Per locked decision: "Auto-retry once silently, then show ErrorStateView with manual retry button if still fails." Ensure retry pattern is consistent.
4. Replace `.font(.kindredHeading1())` etc. with scaled variants.

**PaywallView.swift:**

1. Add @ScaledMetric for typography in paywall.
2. Per locked decision: "Paywall: show cached subscription status only when offline. Disable purchase buttons with 'Go online to manage subscription'." When offline:
   - Disable purchase buttons
   - Show text "Go online to manage subscription" below disabled buttons
   - Still display cached subscription status (tier info)
3. Replace font calls with scaled variants.

**OnboardingView.swift:**

1. Add @ScaledMetric for typography in onboarding steps.
2. Replace font calls with scaled variants.

**FeedReducer.swift:**

1. Add Logger extension for feed:
   ```swift
   import OSLog
   extension Logger {
       private static var subsystem = Bundle.main.bundleIdentifier!
       static let feed = Logger(subsystem: subsystem, category: "feed")
       static let location = Logger(subsystem: subsystem, category: "location")
   }
   ```
2. Replace all `print()` statements with appropriate `Logger.feed` calls.
3. Privacy annotations: recipe IDs and user data `.private`, error descriptions `.public`.

**RecipeDetailReducer.swift:**

1. Replace all `print()` statements with `Logger.feed` calls.
2. Per locked decision: "Auto-retry once silently, then show ErrorStateView with manual retry button." Ensure recipe detail fetch implements auto-retry: on first failure, retry once automatically. On second failure, set error state for ErrorStateView display.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && grep -c "@ScaledMetric" Kindred/Packages/ProfileFeature/Sources/ProfileView.swift && grep -c "Logger\." Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift && grep -c "print(" Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift</automated>
  </verify>
  <done>
- ProfileView: @ScaledMetric typography, ErrorStateView for errors
- PaywallView: @ScaledMetric, offline disable with "Go online to manage subscription"
- OnboardingView: @ScaledMetric typography
- FeedReducer: Logger replaces all print(), auto-retry on first failure
- RecipeDetailReducer: Logger replaces print(), auto-retry pattern
- Zero print() remaining in modified files
  </done>
</task>

</tasks>

<verification>
1. RecipeCardView: grep for @ScaledMetric (3+ instances), no "frame(height: 400"
2. RecipeDetailView: @ScaledMetric present, offline listen disable
3. PaywallView: offline purchase disable present
4. FeedReducer: zero print() statements, Logger calls present
5. RecipeDetailReducer: zero print() statements
6. All views use ErrorStateView for errors (grep for ErrorStateView in modified files)
7. Project builds
</verification>

<success_criteria>
- All modified views use @ScaledMetric typography that scales with Dynamic Type
- RecipeCardView has no fixed height, scrollable at AX sizes, scaled buttons and icons
- Error states use ErrorStateView consistently with retry button
- Offline states show appropriate empty states and disabled actions
- No print() statements remain in modified reducers (replaced with Logger)
</success_criteria>

<output>
After completion, create `.planning/phases/10-accessibility-polish/10-04-SUMMARY.md`
</output>
