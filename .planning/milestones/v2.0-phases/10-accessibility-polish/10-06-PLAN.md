---
phase: 10-accessibility-polish
plan: 06
type: execute
wave: 3
depends_on: ["10-03", "10-04"]
files_modified:
  - Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/SignInStepView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/DietaryPrefsStepView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/LocationStepView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/VoiceTeaserStepView.swift
  - Kindred/Packages/AuthFeature/Sources/SignIn/SignInGateView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Subscription/SubscriptionStatusView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Ads/AdCardView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Ads/BannerAdView.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
  - Kindred/Packages/ProfileFeature/Sources/CulinaryDNASection.swift
  - Kindred/Packages/ProfileFeature/Sources/DietaryPreferencesSection.swift
  - Kindred/Sources/App/RootView.swift
  - Kindred/Sources/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - ACCS-05

must_haves:
  truths:
    - "All user-facing strings in Auth, Monetization, Profile views and RootView use String(localized:)"
    - "Localizable.xcstrings String Catalog exists with English base strings and Turkish translations"
    - "App displays in Turkish when iOS language is set to Turkish"
    - "No hardcoded English strings remain in any view file across the app"
  artifacts:
    - path: "Kindred/Sources/Resources/Localizable.xcstrings"
      provides: "String Catalog with English and Turkish translations for all user-facing strings"
    - path: "Kindred/Packages/AuthFeature/Sources/**/*.swift"
      provides: "String(localized:) for all 6 Auth view files"
    - path: "Kindred/Packages/MonetizationFeature/Sources/**/*.swift"
      provides: "String(localized:) for all 4 Monetization view files"
    - path: "Kindred/Packages/ProfileFeature/Sources/*.swift"
      provides: "String(localized:) for all 3 Profile view files"
  key_links:
    - from: "All view files"
      to: "Localizable.xcstrings"
      via: "String(localized:) initializer -> Xcode auto-extraction -> String Catalog lookup"
      pattern: "String\\(localized:"
---

<objective>
Complete the bilingual localization: extract remaining strings in Auth, Monetization, Profile, and RootView to String(localized:), then create the Localizable.xcstrings String Catalog with English and Turkish translations for ALL strings across the entire app.

Purpose: Completes the String(localized:) migration started in Plan 10-05 (which covered FeedFeature + VoicePlayback), and creates the single app-level String Catalog per locked decision. Turkish-speaking users can use the app in their language.

Output: 14 view files updated with String(localized:); Localizable.xcstrings with English and Turkish translations for all extracted strings across the app.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-accessibility-polish/10-CONTEXT.md
@.planning/phases/10-accessibility-polish/10-RESEARCH.md

# Need Plan 05 SUMMARY to know which strings were extracted
@.planning/phases/10-accessibility-polish/10-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract strings to String(localized:) in Auth, Monetization, Profile, and RootView</name>
  <files>
Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift
Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/SignInStepView.swift
Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/DietaryPrefsStepView.swift
Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/LocationStepView.swift
Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/VoiceTeaserStepView.swift
Kindred/Packages/AuthFeature/Sources/SignIn/SignInGateView.swift
Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift
Kindred/Packages/MonetizationFeature/Sources/Subscription/SubscriptionStatusView.swift
Kindred/Packages/MonetizationFeature/Sources/Ads/AdCardView.swift
Kindred/Packages/MonetizationFeature/Sources/Ads/BannerAdView.swift
Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
Kindred/Packages/ProfileFeature/Sources/CulinaryDNASection.swift
Kindred/Packages/ProfileFeature/Sources/DietaryPreferencesSection.swift
Kindred/Sources/App/RootView.swift
  </files>
  <action>
Continue the String(localized:) extraction for the remaining view files not covered by Plan 10-05. Same replacement rules:

1. `Text("...")` -> `Text(String(localized: "..."))`
2. `Button("...")` -> `Button(String(localized: "..."))`
3. `.accessibilityLabel("...")` -> `.accessibilityLabel(String(localized: "accessibility.view_name.element"))`
4. `.accessibilityHint("...")` -> `.accessibilityHint(String(localized: "accessibility.view_name.hint"))`
5. String interpolation: `"...\(var)..."` -> `String(localized: "...\(var)...")`

**Files to modify (14 files across 4 groups):**

1. **Auth views** (6 files: OnboardingView, SignInStepView, DietaryPrefsStepView, LocationStepView, VoiceTeaserStepView, SignInGateView):
   - All onboarding step titles, descriptions, button labels
   - Accessibility hints added in Plan 10-03
   - Sign-in gate prompt text, button labels
   - Use dotted keys for context-specific: "onboarding.dietary.title", "onboarding.location.subtitle"

2. **Monetization views** (4 files: PaywallView, SubscriptionStatusView, AdCardView, BannerAdView):
   - Pricing text, tier descriptions ("Pro", "Free"), subscription status labels
   - "Go online to manage subscription" offline message
   - Ad attribution text if any

3. **Profile views** (3 files: ProfileView, CulinaryDNASection, DietaryPreferencesSection):
   - Section headers ("Culinary DNA", "Dietary Preferences"), button labels
   - Progress text, subscription status display
   - Settings labels

4. **RootView.swift** (1 file):
   - Tab labels: "Feed", "Me"
   - Offline banner text (if hardcoded here rather than in OfflineBanner component)

**Important:** Do NOT localize developer-facing strings (Logger, debug, UserDefaults keys, GraphQL queries, identifiers).
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && echo "=== Auth views ===" && grep -rl 'String(localized:' Kindred/Packages/AuthFeature/Sources/ 2>/dev/null | wc -l && echo "=== Monetization views ===" && grep -rl 'String(localized:' Kindred/Packages/MonetizationFeature/Sources/ 2>/dev/null | wc -l && echo "=== Profile views ===" && grep -rl 'String(localized:' Kindred/Packages/ProfileFeature/Sources/ 2>/dev/null | wc -l && echo "=== RootView ===" && grep -c 'String(localized:' Kindred/Sources/App/RootView.swift 2>/dev/null</automated>
  </verify>
  <done>
- **Auth views (6 files):** OnboardingView, SignInStepView, DietaryPrefsStepView, LocationStepView, VoiceTeaserStepView, SignInGateView -- all use String(localized:) for onboarding text, button labels, and accessibility hints
- **Monetization views (4 files):** PaywallView, SubscriptionStatusView, AdCardView, BannerAdView -- all use String(localized:) for pricing, tier descriptions, and status labels
- **Profile views (3 files):** ProfileView, CulinaryDNASection, DietaryPreferencesSection -- all use String(localized:) for section headers and labels
- **RootView (1 file):** Tab labels use String(localized:)
- Total: 14 files modified with String(localized:)
- No hardcoded English user-facing strings remain in Auth, Monetization, or Profile view files
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Localizable.xcstrings String Catalog with Turkish translations</name>
  <files>Kindred/Sources/Resources/Localizable.xcstrings</files>
  <action>
Per locked decision: "One app-level String Catalog (.xcstrings) in the main app target -- all strings extracted here."

1. Create `Kindred/Sources/Resources/Localizable.xcstrings` as a JSON file with the iOS 17+ String Catalog format.

2. Set `sourceLanguage: "en"` and include translations for `"en"` and `"tr"`.

3. Generate Turkish translations for ALL strings extracted across Plans 10-05 and 10-06 (Task 1 above). Gather the full list by scanning all view files modified in both plans for String(localized:) keys.

4. Use these translation principles:
   - Keep translations concise (Turkish words average 30% longer than English)
   - Use informal "sen" form (not formal "siz") for a casual cooking app
   - Food/cooking terms: use commonly understood Turkish equivalents
   - UI verbs: "Listen" = "Dinle", "Skip" = "Atla", "Bookmark" = "Kaydet", "Search" = "Ara"

5. Key Turkish translations (common strings):
   - "Feed" = "Akis" (Flow)
   - "Me" = "Ben"
   - "Listen" = "Dinle"
   - "Skip" = "Atla"
   - "Bookmark" = "Kaydet"
   - "Search cities..." = "Sehir ara..."
   - "Use my location" = "Konumumu kullan"
   - "You're offline" = "Cevrimdisi"
   - "Back online" = "Tekrar cevrimici"
   - "Voice narration requires internet" = "Sesli anlatim icin internet gerekli"
   - "Pro" = "Pro"
   - "Free" = "Ucretsiz"
   - "Bookmarks" = "Kaydedilenler"
   - "Dietary Preferences" = "Beslenme Tercihleri"
   - "Culinary DNA" = "Mutfak DNA'si"
   - "Now playing" = "Simdi caliyor"
   - "Paused" = "Durduruldu"
   - "Photo of" = "Fotografi:"
   - "minutes" = "dakika"
   - "calories" = "kalori"
   - "Viral" = "Viral"
   - "Ingredients" = "Malzemeler"
   - "Steps" = "Adimlar"
   - "Sign in" = "Giris yap"

6. Include plurals where needed (e.g., bookmark count):
   ```json
   "bookmarks.count %lld": {
       "localizations": {
           "en": { "variations": { "plural": { "zero": { "stringUnit": { "value": "No bookmarks" } }, "one": { "stringUnit": { "value": "1 bookmark" } }, "other": { "stringUnit": { "value": "%lld bookmarks" } } } } },
           "tr": { "variations": { "plural": { "zero": { "stringUnit": { "value": "Kayit yok" } }, "one": { "stringUnit": { "value": "1 kayit" } }, "other": { "stringUnit": { "value": "%lld kayit" } } } } }
       }
   }
   ```

7. Scan all 32 view files (18 from Plan 10-05 + 14 from this plan) to build the complete string list. Every String(localized:) key must have an entry in this file.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && test -f Kindred/Sources/Resources/Localizable.xcstrings && echo "xcstrings exists" && python3 -c "import json; d=json.load(open('Kindred/Sources/Resources/Localizable.xcstrings')); print(f'sourceLanguage: {d.get(\"sourceLanguage\", \"MISSING\")}'); strings=d.get('strings',{}); print(f'Total strings: {len(strings)}'); tr_count=sum(1 for s in strings.values() if 'tr' in s.get('localizations',{})); print(f'Turkish translations: {tr_count}')"</automated>
  </verify>
  <done>
- Localizable.xcstrings exists at Kindred/Sources/Resources/ with sourceLanguage "en"
- Contains entries for ALL String(localized:) keys from both Plan 10-05 (18 files) and Plan 10-06 (14 files)
- Turkish translations exist for every string entry
- Plurals handled for count-based strings (bookmarks, etc.)
- File is valid JSON in iOS 17+ String Catalog format
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining print() statements to os.log Logger across all reducer files</name>
  <files>All reducer .swift files with remaining print() statements</files>
  <action>
Per locked decision: "Migrate all print() statements to os.log Logger with proper categories."

1. Search ALL files for remaining `print()` statements across these locations:
   - `Kindred/Packages/VoicePlaybackFeature/Sources/` (AudioPlayerManager, VoiceCacheClient, etc.)
   - `Kindred/Packages/AuthFeature/Sources/` (OnboardingReducer, SignInGateReducer, SignInClient, etc.)
   - `Kindred/Packages/MonetizationFeature/Sources/` (SubscriptionReducer, SubscriptionClient, AdClient, etc.)
   - `Kindred/Packages/ProfileFeature/Sources/` (ProfileReducer, PersonalizationClient, etc.)
   - `Kindred/Packages/FeedFeature/Sources/` (LocationClient, LocationManager, NetworkMonitorClient, GuestSessionClient, GuestMigrationClient, etc.)
   - `Kindred/Sources/App/` (any remaining)

2. For each file with print() statements:
   - Add `import OSLog` if not present
   - Add appropriate Logger extension if not already defined:
     ```swift
     extension Logger {
         private static var subsystem = Bundle.main.bundleIdentifier!
         static let {category} = Logger(subsystem: subsystem, category: "{category-name}")
     }
     ```
   - Replace `print("...")` with `Logger.{category}.{level}("...")`:
     - Success messages -> `.info` or `.notice`
     - Error messages -> `.error`
     - Debug/verbose -> `.debug`
     - Warnings -> `.warning`
   - Apply privacy annotations: `.private` for user IDs, recipe IDs, tokens; `.public` for error codes, state names

3. Logger category assignments:
   - VoicePlayback/Audio: `voicePlayback` (category: "voice-playback"), `audioCache` (category: "audio-cache")
   - Auth: `auth` (category: "auth"), `onboarding` (category: "onboarding")
   - Monetization: `subscription` (category: "subscription"), `ads` (category: "ads")
   - Profile: `profile` (category: "profile")
   - Feed: `feed` (category: "feed"), `personalization` (category: "personalization"), `location` (category: "location")
   - Network: `network` (category: "network")
   - Guest: `guestSession` (category: "guest-session")

Note: Some Logger extensions may already exist from Plans 10-02 (appLifecycle, performance, background), 10-03 (voicePlayback in VoicePlaybackReducer), and 10-04 (feed, location in FeedReducer). Only add new extensions where needed. Do not duplicate existing ones.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && echo "=== Remaining print() statements ===" && grep -rl 'print(' Kindred/Packages/*/Sources/**/*.swift Kindred/Sources/App/*.swift 2>/dev/null | wc -l && echo "=== Logger usage ===" && grep -rl 'Logger\.' Kindred/Packages/*/Sources/**/*.swift Kindred/Sources/App/*.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>
- Zero print() statements remain across all feature packages and app sources
- Logger extensions defined for every feature category (voice-playback, audio-cache, auth, onboarding, subscription, ads, profile, feed, personalization, location, network, guest-session)
- Privacy annotations applied to sensitive data in log messages (.private for user IDs, tokens; .public for error codes)
- No duplicate Logger extensions (reuses existing ones from Plans 10-02, 10-03, 10-04)
  </done>
</task>

</tasks>

<verification>
1. At least 6 Auth view files contain String(localized:)
2. At least 4 Monetization view files contain String(localized:)
3. At least 3 Profile view files contain String(localized:)
4. RootView.swift contains String(localized:) for tab labels
5. Localizable.xcstrings exists and is valid JSON with "en" and "tr" localizations
6. Turkish translations are reasonable (spot-check: "Listen" = "Dinle", "Skip" = "Atla")
7. Zero print() statements in Kindred/Packages/ and Kindred/Sources/App/
8. Logger extensions present in feature packages
9. Project builds
</verification>

<success_criteria>
- All remaining view files use String(localized:) for user-facing strings (completing what Plan 10-05 started)
- Localizable.xcstrings has English and Turkish entries for every extracted string across the app
- App displays Turkish text when iOS language is set to Turkish
- All developer logging uses os.log Logger with proper categories and privacy
- No print() statements remain anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/10-accessibility-polish/10-06-SUMMARY.md`
</output>
