---
phase: 09-monetization-voice-tiers
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-07T21:33:00.000Z
---

<current_state>
Phase 9 execution: Plans 01-04 fully executed. Plan 05 Task 2 (human verification) in progress.

This session fixed several bugs. PaywallView is now wired. Banner ad renders with visible debug container. Narration hide/show conditional restored. Testing ongoing.
</current_state>

<completed_work>

## Plans Executed (previous sessions)
- Plan 09-01: MonetizationFeature package (4 commits)
- Plan 09-02: AdMob integration (3 commits)
- Plan 09-03: Feed ad injection, voice slot enforcement (3 commits)
- Plan 09-04: Profile subscription UI, AppDelegate lifecycle, backend JWS (4 commits)
- Plan 09-05 Task 1: Build fixes (1 commit: 19c08bf)

## Fixes Applied This Session (NOT committed yet)

### Bug 1: PaywallView wiring — FIXED
- Added `@Presents var paywall: SubscriptionReducer.State?` to FeedReducer.State
- Added `paywall(PresentationAction<SubscriptionReducer.Action>)` to FeedReducer.Action + manual Equatable
- `.showPaywall` now sets `state.paywall = SubscriptionReducer.State()` (was TODO returning `.none`)
- Added `.ifLet(\.$paywall, action: \.paywall) { SubscriptionReducer() }` to reducer body
- Added `.sheet(item:)` for PaywallView in FeedView
- Added `.onAppear { store.send(.onAppear) }` to PaywallView (was missing — products would never load)
- Files: FeedReducer.swift, FeedView.swift, PaywallView.swift

### Bug 2: First-launch ad suppression — FIXED
- Moved `kindredFirstLaunchComplete` flag from `didFinishLaunchingWithOptions` (set before views → ads on first launch) to `UIApplication.didEnterBackgroundNotification` (set when user backgrounds app → no ads first session)
- File: AppDelegate.swift

### Bug 3: RecipeDetailReducer ad race condition — FIXED
- `subscriptionStatusUpdated` and `adVisibilityDetermined` now treat `.unknown` same as `.free` (only `.pro` suppresses ads) — matches FeedReducer pattern
- File: RecipeDetailReducer.swift

### Bug 4: BannerAdView rewrite — FIXED (ad now loads)
- Original had chicken-and-egg: only rendered GADBannerView when `bannerHeight > 0`, but height starts at 0 so ad never loads
- Removed `@Dependency(\.adClient)` from view (TCA anti-pattern)
- New approach: 60pt ZStack with ProgressView placeholder, BannerViewRepresentable always rendered with proper frame, error state shows red text
- Tracks `adLoaded` bool + `adError` string for debug visibility
- File: BannerAdView.swift

### Debug logging added (REMOVE before final commit)
- RecipeDetailReducer: os.log Logger for subscriptionStatusUpdated, adVisibilityDetermined
- RecipeDetailView: os.log Logger import + detailViewLogger (currently unused after hardcode removal)
- Files: RecipeDetailReducer.swift, RecipeDetailView.swift
</completed_work>

<remaining_work>

## Current Issue: Banner ad doesn't hide during narration
- `shouldShowBannerAd` checks `effectivePlaybackStatus` which reads from `PlaybackObserver.shared.currentPlayback`
- The conditional `if shouldShowBannerAd { BannerAdView() }` is restored
- User reports banner stays visible during narration
- Possible causes:
  1. `PlaybackObserver.shared` not updating when narration starts (check if it's an @Published ObservableObject)
  2. `playback.recipeId` doesn't match `store.recipeId` (ID format mismatch)
  3. `effectivePlaybackStatus` returns `.idle` because currentPlayback is nil
- Next step: Check PlaybackObserver implementation and verify recipeId matching

## Remaining verification checklist
- [x] Native ad cards in feed every 5 recipes (working)
- [x] "Remove ads with Pro" link opens paywall (wired this session)
- [ ] Banner ad in recipe detail (loads but narration hide/show not working)
- [ ] Subscription purchase flow (requires Xcode launch with StoreKit Testing)
- [ ] Pro tier ad suppression
- [ ] Voice slot enforcement
- [ ] Persistence after relaunch
- [ ] First-launch ad suppression

## After verification
- Remove debug logging from RecipeDetailReducer.swift, RecipeDetailView.swift
- Commit all accumulated fixes
- Create 09-05-SUMMARY.md
- Run gsd-verifier
- Update ROADMAP.md
</remaining_work>

<decisions_made>
- Used Google's test ad unit IDs (ca-app-pub-3940256099942544/*) for development
- Treated `.unknown` subscription status same as `.free` for ad visibility (only `.pro` suppresses)
- BannerAdView uses 60pt fixed ZStack with debug error display
- First-launch flag set on background notification (not didFinishLaunching)
- PaywallView gets `.onAppear` to load StoreKit products
</decisions_made>

<blockers>
- Banner ad narration hide/show: `shouldShowBannerAd` conditional not reacting to playback state changes. Need to investigate PlaybackObserver.shared and recipeId matching.
- Subscribe button requires Xcode run with StoreKit Testing config (not CLI install)
</blockers>

<context>
Key files modified this session (all uncommitted):
- Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift (paywall wiring + existing race condition fix)
- Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift (paywall sheet)
- Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailReducer.swift (ad race condition fix + debug logging)
- Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift (debug logging import + shouldShowBannerAd conditional)
- Kindred/Packages/MonetizationFeature/Sources/Ads/BannerAdView.swift (full rewrite - fixed chicken-and-egg)
- Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift (.onAppear added)
- Kindred/Sources/App/AppDelegate.swift (first-launch flag moved to background notification)
</context>

<next_action>
1. Investigate PlaybackObserver.shared — check if it's updating during narration, verify recipeId format matches
2. Fix banner narration hide/show
3. Remove debug logging
4. Commit all fixes
5. Complete 09-05 verification and summary
</next_action>
