---
phase: 27-app-store-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Kindred/Sources/PrivacyInfo.xcprivacy
autonomous: true
requirements:
  - STORE-02
must_haves:
  truths:
    - "PrivacyInfo.xcprivacy declares NSPrivacyCollectedDataTypeSearchHistory (Linked=false, Tracking=false, AppFunctionality)"
    - "PrivacyInfo.xcprivacy shows NSPrivacyCollectedDataTypeProductInteraction with NSPrivacyCollectedDataTypeLinked set to true"
    - "NSPrivacyTrackingDomains array does NOT contain api.spoonacular.com (Spoonacular is a data processor, not a tracker)"
    - "Required Reason API audit completed — existing manifest (UserDefaults CA92.1, FileTimestamp C617.1) is sufficient, no new entries added"
    - "User has a documented checklist to update the App Store Connect App Privacy form to match the manifest before Phase 28 submission"
  artifacts:
    - path: "Kindred/Sources/PrivacyInfo.xcprivacy"
      provides: "Privacy manifest with Search History data type + Product Interaction Linked flip"
      contains: "NSPrivacyCollectedDataTypeSearchHistory"
  key_links:
    - from: "Kindred/Sources/PrivacyInfo.xcprivacy"
      to: "App Store Connect App Privacy form (manual)"
      via: "user copies manifest data types into ASC web form before Phase 28 submission"
      pattern: "NSPrivacyCollectedDataTypeSearchHistory"
---

<objective>
Update `Kindred/Sources/PrivacyInfo.xcprivacy` to disclose Spoonacular-related data collection and correct the existing Product Interaction entry. Also document the Required Reason API audit result and the manual App Store Connect checklist the user must follow after the manifest lands.

Purpose: STORE-02 requires Privacy Labels that accurately reflect Spoonacular backend integration (search queries) and Firebase Analytics user-id linking. The manifest is the source of truth that App Store Connect labels derive from since iOS 17+. Misclassifying Spoonacular as a tracking domain triggers review warnings; omitting Search History misrepresents what leaves the device.

Output:
- Edited `Kindred/Sources/PrivacyInfo.xcprivacy` with 8 data type entries (was 7) and Product Interaction corrected to Linked=true
- Audit note confirming no new Required Reason API entries needed
- Manual App Store Connect checklist surfaced in the plan's `<done>` criteria for the user to execute before Phase 28
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-app-store-compliance/27-CONTEXT.md
@.planning/phases/27-app-store-compliance/27-RESEARCH.md
@Kindred/Sources/PrivacyInfo.xcprivacy

<interfaces>
<!-- Exact structure of the existing manifest (verified 2026-04-06). -->
<!-- Executor should edit in place; do not regenerate the whole file. -->

Current `NSPrivacyCollectedDataTypes` array contents (Kindred/Sources/PrivacyInfo.xcprivacy):
  1. NSPrivacyCollectedDataTypeAudioData        — Linked=true,  Tracking=false, AppFunctionality (lines 21-33)
  2. NSPrivacyCollectedDataTypeCoarseLocation   — Linked=false, Tracking=false, AppFunctionality (lines 35-47)
  3. NSPrivacyCollectedDataTypeEmailAddress     — Linked=true,  Tracking=false, AppFunctionality (lines 49-61)
  4. NSPrivacyCollectedDataTypeUserID           — Linked=true,  Tracking=false, AppFunctionality (lines 63-75)
  5. NSPrivacyCollectedDataTypeProductInteraction — Linked=**false** (WILL FLIP TO true), Tracking=false, Analytics (lines 77-89)
  6. NSPrivacyCollectedDataTypeCrashData        — Linked=false, Tracking=false, AppFunctionality (lines 91-103)
  7. NSPrivacyCollectedDataTypePurchaseHistory  — Linked=true,  Tracking=false, AppFunctionality (lines 105-117)
  (NEW) 8. NSPrivacyCollectedDataTypeSearchHistory — Linked=false, Tracking=false, AppFunctionality

Current `NSPrivacyAccessedAPITypes` array contents (lines 121-142):
  - NSPrivacyAccessedAPICategoryUserDefaults with reason code CA92.1
  - NSPrivacyAccessedAPICategoryFileTimestamp with reason code C617.1

Current `NSPrivacyTrackingDomains` array (lines 9-16):
  - googleads.g.doubleclick.net
  - pagead2.googlesyndication.com
  - www.googleadservices.com
  - www.google-analytics.com
  - app-measurement.com
  (MUST NOT add api.spoonacular.com — Spoonacular is a data processor, not a tracker.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Search History data type + flip Product Interaction to Linked=true</name>
  <files>Kindred/Sources/PrivacyInfo.xcprivacy</files>
  <action>
Make two targeted edits to `Kindred/Sources/PrivacyInfo.xcprivacy`:

**Edit A — Flip Product Interaction Linked flag (line 82):**
Inside the `NSPrivacyCollectedDataTypeProductInteraction` dict (currently lines 78-89), change:
```xml
<key>NSPrivacyCollectedDataTypeLinked</key>
<false/>
```
to:
```xml
<key>NSPrivacyCollectedDataTypeLinked</key>
<true/>
```
Add an inline XML comment immediately above the `<key>NSPrivacyCollectedDataTypeLinked</key>` line:
```xml
<!-- Linked=true: Firebase Analytics receives Clerk user id via user properties. Decision stands per Phase 27 CONTEXT.md even though the wiring is not yet confirmed in code — manual ASC re-verification required (see plan's done criteria). -->
```
Do NOT change the purpose (stays `NSPrivacyCollectedDataTypePurposeAnalytics`). Do NOT broaden purposes.

**Edit B — Append Search History data type (after line 117, before closing `</array>` at line 118):**
Insert a new `<dict>` block as the 8th entry in `NSPrivacyCollectedDataTypes`, preserving the file's tab indentation (tabs, not spaces — match the existing file style):
```xml
		<!-- 8. Search History (Spoonacular recipe queries + filter parameters) -->
		<!-- Spoonacular is a DATA PROCESSOR, not a tracker. Queries are proxied through api.kindredcook.app and are not linked to the user's identifier. Filter parameters (cuisine, diet, intolerances) fold into Search History per Apple's definition. Do NOT add api.spoonacular.com to NSPrivacyTrackingDomains. -->
		<dict>
			<key>NSPrivacyCollectedDataType</key>
			<string>NSPrivacyCollectedDataTypeSearchHistory</string>
			<key>NSPrivacyCollectedDataTypeLinked</key>
			<false/>
			<key>NSPrivacyCollectedDataTypeTracking</key>
			<false/>
			<key>NSPrivacyCollectedDataTypePurposes</key>
			<array>
				<string>NSPrivacyCollectedDataTypePurposeAppFunctionality</string>
			</array>
		</dict>
```

**DO NOT:**
- Modify any other data type entry (Audio Data, Coarse Location, Email, User ID, Crash Data, Purchase History).
- Add any new entries to `NSPrivacyTrackingDomains`. Spoonacular is NOT a tracker.
- Add any new entries to `NSPrivacyAccessedAPITypes` (see Task 2 for audit documentation).
- Reformat or re-indent other parts of the file. Use targeted edits only.
- Change any values in `NSPrivacyTracking` (stays `<true/>`).

After edits, the file must remain a valid plist with exactly 8 `NSPrivacyCollectedDataTypes` entries and exactly 2 `NSPrivacyAccessedAPITypes` entries.
  </action>
  <verify>
    <automated>plutil -lint Kindred/Sources/PrivacyInfo.xcprivacy && xmllint --noout Kindred/Sources/PrivacyInfo.xcprivacy && plutil -p Kindred/Sources/PrivacyInfo.xcprivacy | grep -c "NSPrivacyCollectedDataType " | grep -q "^8$" && plutil -p Kindred/Sources/PrivacyInfo.xcprivacy | grep -q "NSPrivacyCollectedDataTypeSearchHistory"</automated>
  </verify>
  <done>
- `plutil -lint` exits 0 with "OK"
- `xmllint --noout` exits 0 with no errors
- `plutil -p Kindred/Sources/PrivacyInfo.xcprivacy` shows exactly 8 NSPrivacyCollectedDataType entries
- `NSPrivacyCollectedDataTypeSearchHistory` is present with Linked=false, Tracking=false, AppFunctionality purpose
- `NSPrivacyCollectedDataTypeProductInteraction` dict shows `NSPrivacyCollectedDataTypeLinked => 1` (true)
- `NSPrivacyTrackingDomains` array still has exactly 5 entries (no Spoonacular entry)
- `NSPrivacyAccessedAPITypes` array still has exactly 2 entries (UserDefaults, FileTimestamp)
  </done>
</task>

<task type="auto">
  <name>Task 2: Document Required Reason API audit result + App Store Connect checklist in summary</name>
  <files>.planning/phases/27-app-store-compliance/27-01-SUMMARY.md</files>
  <action>
When this plan completes, the SUMMARY.md this plan writes MUST include an explicit section titled "Required Reason API Audit Result" containing:

1. **Audit scope:** The 5 Apple categories (UserDefaults, FileTimestamp, DiskSpace, SystemBootTime, ActiveKeyboards) across Kindred first-party code AND SPM dependencies.
2. **Audit result (per 27-RESEARCH.md):** "No new entries required. Existing manifest declares UserDefaults (CA92.1) and FileTimestamp (C617.1); grep of Kindred first-party code found no usage of DiskSpace APIs (volumeAvailableCapacityKey, availableCapacity), SystemBootTime APIs (systemUptime, mach_absolute_time, kern.boottime), or ActiveKeyboards (UITextInputMode). SPM dependencies (Firebase, TCA, Apollo, Clerk, Kingfisher) ship their own privacy manifests."
3. **Rationale for not adding entries:** Adding fictional reason codes risks rejection for inaccurate declarations. If App Store Connect upload returns ITMS-91053 for specific API types after Phase 28 upload, the executor should add reason codes reactively with the exact codes Apple reports.

The SUMMARY.md MUST also include an explicit section titled "MANUAL CHECKLIST — App Store Connect (user must perform before Phase 28)" containing the following numbered steps verbatim:

```
1. Log in to https://appstoreconnect.apple.com → My Apps → Kindred → App Privacy.
2. Click "Edit" in the "Data Types" section.
3. Under "Data Not Linked to You," ADD "Search History" with these settings:
   - Purposes: App Functionality ONLY (do not check Analytics, Product Personalization, App Advertising, or Other Purposes)
   - Used for Tracking: No
4. Locate "Product Interaction" in the existing data types list. It is currently filed under "Data Not Linked to You." MOVE it to "Data Linked to You":
   - Purposes: Analytics ONLY (do not broaden)
   - Used for Tracking: No
5. Confirm "Coarse Location" stays under "Data Not Linked to You" (unchanged).
6. Confirm the "Data Used to Track You" section is EMPTY for Spoonacular — do NOT add api.spoonacular.com to any tracking list.
7. Click "Save" and verify the preview matches PrivacyInfo.xcprivacy (8 data types, 1 tracking section unchanged).
8. Confirmation: reply "ASC privacy form updated on YYYY-MM-DD" in the Phase 27 verification report before running Phase 28.
```

**Important:** This SUMMARY.md is the plan's output artifact — it serves both as the plan's closure doc and as the user's execution checklist. The executor writes this SUMMARY.md after completing Task 1.
  </action>
  <verify>
    <automated>test -f .planning/phases/27-app-store-compliance/27-01-SUMMARY.md && grep -q "Required Reason API Audit Result" .planning/phases/27-app-store-compliance/27-01-SUMMARY.md && grep -q "App Store Connect" .planning/phases/27-app-store-compliance/27-01-SUMMARY.md && grep -q "Search History" .planning/phases/27-app-store-compliance/27-01-SUMMARY.md && grep -q "Product Interaction" .planning/phases/27-app-store-compliance/27-01-SUMMARY.md</automated>
  </verify>
  <done>
- `27-01-SUMMARY.md` exists in `.planning/phases/27-app-store-compliance/`
- Contains a "Required Reason API Audit Result" section documenting no new entries needed
- Contains a "MANUAL CHECKLIST — App Store Connect" section with 8 numbered steps
- Checklist explicitly names Search History and Product Interaction
- Checklist explicitly warns NOT to add api.spoonacular.com to tracking domains
  </done>
</task>

</tasks>

<verification>
Full plan verification combines automated syntax checks with a manual checklist the user must acknowledge:

**Automated (must pass):**
- `plutil -lint Kindred/Sources/PrivacyInfo.xcprivacy` returns OK
- `xmllint --noout Kindred/Sources/PrivacyInfo.xcprivacy` returns no errors
- `plutil -p Kindred/Sources/PrivacyInfo.xcprivacy` shows 8 data types including Search History
- Product Interaction dict shows Linked=1 (true)
- NSPrivacyTrackingDomains still has 5 entries, none containing "spoonacular"
- `27-01-SUMMARY.md` contains the audit result and App Store Connect checklist sections

**Human (surfaced via SUMMARY.md checklist):**
- User opens App Store Connect, adds Search History (Not Linked, App Functionality only), moves Product Interaction to Linked, confirms tracking section unchanged, and records the confirmation date in the Phase 27 verification report before running Phase 28.

**Known gap (surfaced in SUMMARY.md):**
Firebase Analytics user-id wiring was NOT found via grep in the codebase (`setUserId`, `setUserProperty`, `Analytics.logEvent` with user params — no matches). The decision to flip Product Interaction to Linked=true stands per CONTEXT.md, but the user should re-confirm this reflects actual app behavior after Phase 27 merges. If the wiring is genuinely absent, revisit the decision in a follow-up phase.
</verification>

<success_criteria>
- `Kindred/Sources/PrivacyInfo.xcprivacy` passes `plutil -lint` and `xmllint --noout`
- Manifest declares 8 NSPrivacyCollectedDataTypes (7 original + Search History)
- Product Interaction is `Linked=true` (was `false`)
- NSPrivacyTrackingDomains is unchanged (no Spoonacular entry)
- NSPrivacyAccessedAPITypes is unchanged (no fictional entries added)
- `27-01-SUMMARY.md` documents the Required Reason API audit result and the App Store Connect manual checklist
- STORE-02's privacy-manifest portion is satisfied; the ASC and policy portions are satisfied by Plan 27-03
</success_criteria>

<output>
After completion, create `.planning/phases/27-app-store-compliance/27-01-SUMMARY.md` with:
1. Changes made (exact plist edits with line references)
2. Automated verification output (plutil, xmllint, plutil -p snippets)
3. "Required Reason API Audit Result" section (see Task 2)
4. "MANUAL CHECKLIST — App Store Connect" section (see Task 2)
5. "Known gap: Firebase user-id wiring" note for follow-up
</output>
