---
phase: 08-authentication-onboarding
plan: 04
type: execute
wave: 3
depends_on:
  - "08-02"
  - "08-03"
files_modified: []
autonomous: false
requirements:
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "Human verifies onboarding carousel flows through all steps on device"
    - "Human verifies auth gate appears when guest taps bookmark or listen"
    - "Human verifies sign-in works with Apple or Google on device"
    - "Human verifies gate cooldown and deferred action behavior"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete authentication and onboarding flow on a real device. Clerk OAuth requires device testing (not simulator) for full verification.

Purpose: Ensure all Phase 8 flows work correctly end-to-end before marking phase complete.
Output: Verified auth gate, onboarding, sign-in, and migration functionality.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-authentication-onboarding/08-01-SUMMARY.md
@.planning/phases/08-authentication-onboarding/08-02-SUMMARY.md
@.planning/phases/08-authentication-onboarding/08-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify onboarding flow on device</name>
  <files>N/A (verification only)</files>
  <action>
    Human verifies onboarding carousel on device.

    What was built: Onboarding carousel replaces WelcomeCardView on first launch. Four horizontal steps with dots indicator: Sign-in, Dietary Preferences, Location, Voice Teaser. All steps skippable.

    How to verify:
    1. Delete app from device (or reset UserDefaults) to trigger first-launch
    2. Build and run: `xcodebuild -scheme Kindred -destination 'id=00008140-00125CDC0152801C' build`
    3. After splash screen, onboarding carousel should appear (NOT the old WelcomeCardView)
    4. Verify horizontal swipe between 4 pages with dots indicator at bottom
    5. Step 1 (Sign-in): Apple + Google buttons visible, "Continue as guest" link at bottom
    6. Tap "Skip" or "Continue as guest" to advance
    7. Step 2 (Dietary): Chip grid visible, can select/deselect multiple chips
    8. Step 3 (Location): "Use my location" and "Enter city manually" buttons visible
    9. Step 4 (Voice): Teaser text with "Try it now" / "Set up later" buttons
    10. Complete onboarding, then force-quit and relaunch — onboarding should NOT reappear

    Resume signal: Type "approved" or describe issues
  </action>
  <verify>
    <automated>xcodebuild build -scheme Kindred -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves onboarding flow: carousel with 4 skippable steps, dots indicator, completion persistence across restarts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify auth gate and sign-in flow on device</name>
  <files>N/A (verification only)</files>
  <action>
    Human verifies auth gate and sign-in on device.

    What was built: Auth gate appears as full-screen cover when guest user taps bookmark or listen. Apple Sign In and Google OAuth work via Clerk SDK. After sign-in, deferred action auto-completes. 5-minute cooldown after gate dismissal.

    How to verify:
    1. Launch app as guest (skip onboarding sign-in)
    2. Browse feed — should work normally without auth gate
    3. Try to bookmark a recipe (swipe right or tap bookmark on detail) — auth gate should appear
    4. Verify auth gate layout: Apple button on top, Google below, branding + tagline, "Continue as guest" at bottom
    5. Dismiss the gate (swipe down or tap "Continue as guest")
    6. Try to bookmark again immediately — gate should NOT appear (5-minute cooldown)
    7. Try to tap "Listen" on recipe detail — auth gate should appear (listen is also gated)
    8. Attempt sign-in with Apple or Google (Clerk SDK requires device, not simulator)
    9. After successful sign-in: verify you return to where you were and the original action completes
    10. Verify no lock icons visible on bookmark/listen buttons before tapping

    Resume signal: Type "approved" or describe issues
  </action>
  <verify>
    <automated>xcodebuild build -scheme Kindred -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves auth gate flow: full-screen gate on gated actions, cooldown works, sign-in succeeds, deferred action auto-completes, no lock icons on buttons</done>
</task>

</tasks>

<verification>
1. Onboarding replaces WelcomeCardView (not shown simultaneously)
2. All 4 onboarding steps render correctly
3. Onboarding completion persists across restarts
4. Auth gate appears for gated actions (bookmark, listen)
5. Auth gate does NOT appear for non-gated actions (browse, skip, view)
6. Gate cooldown prevents immediate re-show
7. Sign-in succeeds with at least one provider (Apple or Google)
8. Deferred action auto-completes after sign-in
9. No visual indicators on gated buttons
</verification>

<success_criteria>
- Human approves both checkpoints
- Onboarding and auth gate work correctly on physical device
- All Phase 8 requirements (AUTH-02 through AUTH-06) verified
</success_criteria>

<output>
After completion, create `.planning/phases/08-authentication-onboarding/08-04-SUMMARY.md`
</output>
