---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/nest-cli.json
  - backend/prisma/schema.prisma
  - backend/src/main.ts
  - backend/src/app.module.ts
  - backend/src/config/config.module.ts
  - backend/src/config/env.validation.ts
  - backend/src/common/filters/all-exceptions.filter.ts
  - backend/src/common/decorators/current-user.decorator.ts
  - backend/src/prisma/prisma.module.ts
  - backend/src/prisma/prisma.service.ts
  - backend/src/graphql/models/recipe.model.ts
  - backend/src/graphql/models/user.model.ts
  - backend/src/graphql/models/ingredient.model.ts
  - backend/src/graphql/models/recipe-step.model.ts
  - backend/src/graphql/models/device-token.model.ts
  - backend/src/recipes/recipes.module.ts
  - backend/src/recipes/recipes.resolver.ts
  - backend/src/recipes/recipes.service.ts
  - backend/src/users/users.module.ts
  - backend/src/users/users.resolver.ts
  - backend/src/users/users.service.ts
  - backend/src/health/health.module.ts
  - backend/src/health/health.resolver.ts
  - backend/Dockerfile
  - backend/docker-compose.yml
  - backend/.env.example
  - backend/.gitignore
  - backend/.dockerignore
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "NestJS application boots and responds to GraphQL queries on /v1/graphql"
    - "Prisma schema defines User, Recipe, Ingredient, RecipeStep, DeviceToken models with proper relations"
    - "Docker Compose starts PostgreSQL + NestJS app with a single command"
    - "GraphQL Playground accessible in development mode for schema exploration"
    - "Recipe query returns typed data matching the code-first schema"
  artifacts:
    - path: "backend/src/main.ts"
      provides: "NestJS bootstrap with /v1/graphql endpoint"
      min_lines: 20
    - path: "backend/prisma/schema.prisma"
      provides: "Database schema for all Phase 1 models"
      contains: "model Recipe"
    - path: "backend/src/app.module.ts"
      provides: "Root module wiring GraphQL, Prisma, feature modules"
      min_lines: 30
    - path: "backend/Dockerfile"
      provides: "Multi-stage Docker build for production"
      contains: "FROM node:20-alpine"
    - path: "backend/docker-compose.yml"
      provides: "Local dev environment with PostgreSQL"
      contains: "postgres"
    - path: "backend/src/graphql/models/recipe.model.ts"
      provides: "Code-first GraphQL Recipe type"
      exports: ["Recipe"]
  key_links:
    - from: "backend/src/app.module.ts"
      to: "backend/src/prisma/prisma.module.ts"
      via: "NestJS module import"
      pattern: "PrismaModule"
    - from: "backend/src/recipes/recipes.service.ts"
      to: "backend/src/prisma/prisma.service.ts"
      via: "DI injection"
      pattern: "PrismaService"
    - from: "backend/src/recipes/recipes.resolver.ts"
      to: "backend/src/graphql/models/recipe.model.ts"
      via: "GraphQL return type"
      pattern: "Recipe"
---

<objective>
Scaffold the NestJS backend project with PostgreSQL database, Prisma ORM, code-first GraphQL API, and Docker containerization. This is the foundation that every subsequent plan builds on.

Purpose: Establish the runnable backend skeleton with database schema, GraphQL endpoint, and local development environment so all other Phase 1 work (auth, scraping, AI images, push) has a stable base to build into.
Output: A working NestJS app with GraphQL Playground, Prisma schema with all required models, Docker Compose for local dev, and multi-stage Dockerfile for production.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NestJS scaffold, dependencies, and infrastructure files</name>
  <files>
    backend/package.json, backend/tsconfig.json, backend/nest-cli.json,
    backend/Dockerfile, backend/docker-compose.yml,
    backend/.env.example, backend/.gitignore, backend/.dockerignore
  </files>
  <action>
    1. Create `backend/` directory at project root. Initialize NestJS project using `@nestjs/cli`:
       ```
       npx @nestjs/cli new backend --package-manager npm --skip-git
       ```
       Then install all dependencies from RESEARCH.md Standard Stack (core + supporting).

    2. Create `backend/Dockerfile` with multi-stage build:
       - Stage 1 (builder): node:20-alpine, npm ci, prisma generate, npm run build
       - Stage 2 (production): node:20-alpine, non-root user (nestjs:1001), copy dist + node_modules + prisma, EXPOSE 3000, CMD ["node", "dist/main"]
       Target image size: < 250MB.

    3. Create `backend/docker-compose.yml`:
       - postgres service: postgres:15-alpine, port 5432, volume for persistence, healthcheck
       - app service: build from Dockerfile, port 3000, depends_on postgres, env from .env

    4. Create `backend/.env.example` with all env vars documented (values empty, comments explaining where to get each): DATABASE_URL, NODE_ENV, PORT, CLERK_SECRET_KEY, CLERK_PUBLISHABLE_KEY, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, CLOUDFLARE_ACCOUNT_ID, GOOGLE_CLOUD_PROJECT, FIREBASE_SERVICE_ACCOUNT_PATH.

    5. Create `backend/.gitignore` (node_modules, dist, .env, prisma/*.db) and `backend/.dockerignore` (node_modules, dist, .git, .env).
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred/backend && test -f package.json && test -f Dockerfile && test -f docker-compose.yml && test -f .env.example && echo "All scaffold files exist"</automated>
  </verify>
  <done>NestJS project initialized with all dependencies installed, Dockerfile with multi-stage build, docker-compose.yml with PostgreSQL, .env.example with all documented vars, .gitignore and .dockerignore created.</done>
</task>

<task type="auto">
  <name>Task 2: Prisma schema with all models, config module, and common utilities</name>
  <files>
    backend/prisma/schema.prisma,
    backend/src/prisma/prisma.module.ts, backend/src/prisma/prisma.service.ts,
    backend/src/config/config.module.ts, backend/src/config/env.validation.ts,
    backend/src/common/filters/all-exceptions.filter.ts,
    backend/src/common/decorators/current-user.decorator.ts
  </files>
  <action>
    1. Create `backend/prisma/schema.prisma` with PostgreSQL provider and these models:
       - **User**: id (cuid), clerkId (String @unique), email (String @unique), displayName (String?), createdAt, updatedAt, relations to bookmarks (Recipe[]), deviceTokens (DeviceToken[])
       - **Recipe**: id (cuid), name (String), description (String?), prepTime (Int, minutes), cookTime (Int?, minutes), servings (Int?), calories (Int?), protein (Float?), carbs (Float?), fat (Float?), difficulty (enum: BEGINNER/INTERMEDIATE/ADVANCED), imageUrl (String?), imageStatus (enum: PENDING/GENERATING/COMPLETED/FAILED), scrapedFrom (String: 'x'|'instagram'|'curated'), sourceId (String? @unique for dedup), scrapedAt (DateTime), location (String, city-level), engagementLoves (Int @default(0)), engagementBookmarks (Int @default(0)), engagementViews (Int @default(0)), isViral (Boolean @default(false)), dietaryTags (String[]), createdAt, updatedAt
       - **Ingredient**: id (cuid), recipeId (relation to Recipe), name (String, normalized), quantity (String), unit (String), orderIndex (Int)
       - **RecipeStep**: id (cuid), recipeId (relation to Recipe), orderIndex (Int), text (String), duration (Int?, seconds), techniqueTag (String?)
       - **DeviceToken**: id (cuid), userId (relation to User), token (String @unique), platform (enum: IOS/ANDROID), createdAt, updatedAt
       - **Bookmark**: composite relation table between User and Recipe with createdAt
       Add appropriate indexes: Recipe(location), Recipe(scrapedAt), Recipe(isViral), Ingredient(recipeId), RecipeStep(recipeId).

    2. Create `backend/src/prisma/prisma.service.ts` extending PrismaClient with onModuleInit (connect) and enableShutdownHooks. Create `backend/src/prisma/prisma.module.ts` as global module exporting PrismaService.

    3. Create `backend/src/config/config.module.ts` using @nestjs/config with env validation. Create `backend/src/config/env.validation.ts` using class-validator to validate: DATABASE_URL, NODE_ENV, PORT (default 3000), CLERK_SECRET_KEY, CLERK_PUBLISHABLE_KEY, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, CLOUDFLARE_ACCOUNT_ID, GOOGLE_CLOUD_PROJECT, FIREBASE_SERVICE_ACCOUNT_PATH. Mark external service keys as optional for local dev (validated at service level instead).

    4. Create `backend/src/common/filters/all-exceptions.filter.ts` -- global exception filter that logs errors with structured format (timestamp, path, statusCode, message) and returns GraphQL-friendly error responses.

    5. Create `backend/src/common/decorators/current-user.decorator.ts` -- custom param decorator that extracts user from GraphQL context (to be populated by auth guard in Plan 02).

    6. Run `npx prisma generate` and `npx prisma db push` against Docker Postgres to verify schema.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred/backend && npx prisma validate && npx tsc --noEmit</automated>
  </verify>
  <done>Prisma schema validates with all 6 models (User, Recipe, Ingredient, RecipeStep, DeviceToken, Bookmark) and proper indexes. PrismaModule is global. Config validates env vars. Exception filter and CurrentUser decorator ready for use. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: GraphQL code-first schema, resolvers, and health check</name>
  <files>
    backend/src/app.module.ts,
    backend/src/main.ts,
    backend/src/graphql/models/recipe.model.ts,
    backend/src/graphql/models/user.model.ts,
    backend/src/graphql/models/ingredient.model.ts,
    backend/src/graphql/models/recipe-step.model.ts,
    backend/src/graphql/models/device-token.model.ts,
    backend/src/recipes/recipes.module.ts,
    backend/src/recipes/recipes.resolver.ts,
    backend/src/recipes/recipes.service.ts,
    backend/src/users/users.module.ts,
    backend/src/users/users.resolver.ts,
    backend/src/users/users.service.ts,
    backend/src/health/health.module.ts,
    backend/src/health/health.resolver.ts
  </files>
  <action>
    1. Create GraphQL model files using @nestjs/graphql decorators (code-first approach):
       - `recipe.model.ts`: @ObjectType Recipe with all fields from Prisma model, @Field decorators with proper types (ID, Int, Float, [String], nullable where appropriate). Include nested Ingredient[] and RecipeStep[] fields.
       - `ingredient.model.ts`: @ObjectType Ingredient (name, quantity, unit, orderIndex)
       - `recipe-step.model.ts`: @ObjectType RecipeStep (orderIndex, text, duration, techniqueTag)
       - `user.model.ts`: @ObjectType User (id, email, displayName, createdAt). Do NOT expose clerkId.
       - `device-token.model.ts`: @ObjectType DeviceToken (id, platform, createdAt)
       Create GraphQL enums: DifficultyLevel, Platform, ImageStatus using registerEnumType.

    2. Create `backend/src/recipes/recipes.service.ts`:
       - Inject PrismaService
       - `findAll(location?: string, limit?: number, offset?: number)`: Query recipes with include for ingredients and steps, filter by location if provided, order by scrapedAt desc, default limit 20
       - `findById(id: string)`: Single recipe with all relations
       - `findViral(location: string)`: Recipes where isViral=true for location, limit 10
       - These are read-only for now; scraping service (Plan 03) will handle creation.

    3. Create `backend/src/recipes/recipes.resolver.ts`:
       - @Resolver(() => Recipe)
       - @Query: `recipes(location?: String, limit?: Int, offset?: Int) => [Recipe]`
       - @Query: `recipe(id: ID!) => Recipe` (nullable, returns null if not found)
       - @Query: `viralRecipes(location: String!) => [Recipe]`
       - Use @Args decorators with proper GraphQL types.

    4. Create `backend/src/recipes/recipes.module.ts` importing PrismaModule, providing RecipesService and RecipesResolver.

    5. Create `backend/src/users/users.service.ts`:
       - Inject PrismaService
       - `findByClerkId(clerkId: string)`: Find user by Clerk ID
       - `findById(id: string)`: Find user by internal ID
       - `createFromClerk(clerkId: string, email: string, displayName?: string)`: Create user from Clerk webhook

    6. Create `backend/src/users/users.resolver.ts`:
       - @Resolver(() => User)
       - @Query: `me => User` (placeholder, will be wired to auth in Plan 02)

    7. Create `backend/src/users/users.module.ts`.

    8. Create `backend/src/health/health.resolver.ts`:
       - @Query: `health => String` returns "ok"
       - @Query: `dbHealth => Boolean` runs `prisma.$queryRaw('SELECT 1')` and returns true/false

    9. Create `backend/src/health/health.module.ts`.

    10. Update `backend/src/app.module.ts` to import:
        - ConfigModule (global, with env validation)
        - GraphQLModule.forRoot with ApolloDriver: autoSchemaFile 'schema.gql', playground true, path '/v1/graphql', subscriptions with graphql-ws enabled, context passing req
        - PrismaModule (global)
        - RecipesModule, UsersModule, HealthModule
        - ThrottlerModule with default 60s/100 requests

    11. Update `backend/src/main.ts`:
        - Bootstrap with NestFactory.create
        - Set global prefix to empty (GraphQL path handles versioning)
        - Enable CORS for mobile clients (allow all origins in dev, restrict in prod)
        - Set body parser limit to 10mb (for future voice upload proxy)
        - Apply global exception filter
        - Listen on PORT from config (default 3000)
        - Log startup with URL to GraphQL Playground

    12. Start Docker Compose, run prisma migrate dev, and verify GraphQL Playground loads at http://localhost:3000/v1/graphql with the health query returning "ok".
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred/backend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>GraphQL Playground accessible at /v1/graphql, health query returns "ok", recipes query returns empty array, schema.gql auto-generated with Recipe/User/Ingredient/RecipeStep types, Docker Compose starts both Postgres and app successfully.</done>
</task>

</tasks>

<verification>
1. `cd backend && docker compose up -d` starts PostgreSQL and NestJS app
2. `curl http://localhost:3000/v1/graphql` responds (not 404)
3. GraphQL query `{ health }` returns `"ok"`
4. GraphQL query `{ recipes { id name } }` returns empty array (no recipes yet)
5. `npx prisma validate` passes
6. `npx tsc --noEmit` passes with zero errors
7. `docker build -t kindred-backend .` produces image < 250MB
</verification>

<success_criteria>
- NestJS app boots and serves GraphQL at /v1/graphql
- Prisma schema has User, Recipe, Ingredient, RecipeStep, DeviceToken models
- Code-first GraphQL types match Prisma models
- Docker Compose provides one-command local dev
- Dockerfile produces optimized production image
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
