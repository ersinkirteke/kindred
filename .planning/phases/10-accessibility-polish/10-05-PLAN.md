---
phase: 10-accessibility-polish
plan: 05
type: execute
wave: 3
depends_on: ["10-03", "10-04"]
files_modified:
  - Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/SwipeCardStack.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/DietaryChipBar.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/DietaryChip.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/ViralBadge.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/ForYouBadge.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/EndOfStackCard.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/CardCountIndicator.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/IngredientChecklistView.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/StepTimelineView.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/ParallaxHeader.swift
  - Kindred/Packages/FeedFeature/Sources/Location/LocationPickerView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/MiniPlayerView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePickerView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/VoiceUpload/VoiceUploadView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/SignInStepView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/DietaryPrefsStepView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/LocationStepView.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/Steps/VoiceTeaserStepView.swift
  - Kindred/Packages/AuthFeature/Sources/SignIn/SignInGateView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Subscription/SubscriptionStatusView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Ads/AdCardView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Ads/BannerAdView.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
  - Kindred/Packages/ProfileFeature/Sources/CulinaryDNASection.swift
  - Kindred/Packages/ProfileFeature/Sources/DietaryPreferencesSection.swift
  - Kindred/Sources/App/RootView.swift
  - Kindred/Sources/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - ACCS-05

must_haves:
  truths:
    - "All user-facing strings use String(localized:) for localization support"
    - "Localizable.xcstrings String Catalog exists with English base strings"
    - "Turkish translations exist for all extracted strings"
    - "App displays in Turkish when iOS language is set to Turkish"
    - "No hardcoded English strings remain in any view file"
    - "All remaining print() statements across all features replaced with os.log Logger"
  artifacts:
    - path: "Kindred/Sources/Resources/Localizable.xcstrings"
      provides: "String Catalog with English and Turkish translations for all user-facing strings"
    - path: "All view .swift files"
      provides: "String(localized:) for every user-facing string"
  key_links:
    - from: "All view files"
      to: "Localizable.xcstrings"
      via: "String(localized:) initializer → Xcode auto-extraction → String Catalog lookup"
      pattern: "String\\(localized:"
---

<objective>
Full bilingual localization (English + Turkish) with String Catalog, plus final print()-to-Logger migration across all feature reducers. Every user-facing string is extracted via String(localized:), Turkish translations are generated, and all debug logging uses os.log Logger.

Purpose: Enables Turkish-speaking users to use the app in their language (per locked decision: "English + Turkish bilingual support for initial launch"). Completes the logging migration for production-quality diagnostics.

Output: Localizable.xcstrings with all strings in English and Turkish; all view files using String(localized:); all reducers using Logger instead of print().
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-accessibility-polish/10-CONTEXT.md
@.planning/phases/10-accessibility-polish/10-RESEARCH.md

# Need prior plan summaries to know what changed
@.planning/phases/10-accessibility-polish/10-03-SUMMARY.md
@.planning/phases/10-accessibility-polish/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract all user-facing strings to String(localized:) across all view files</name>
  <files>
All view .swift files listed in files_modified (FeedView, RecipeCardView, SwipeCardStack, DietaryChipBar, DietaryChip, ViralBadge, ForYouBadge, EndOfStackCard, CardCountIndicator, RecipeDetailView, IngredientChecklistView, StepTimelineView, ParallaxHeader, LocationPickerView, MiniPlayerView, ExpandedPlayerView, VoicePickerView, VoiceUploadView, OnboardingView, SignInStepView, DietaryPrefsStepView, LocationStepView, VoiceTeaserStepView, SignInGateView, PaywallView, SubscriptionStatusView, AdCardView, BannerAdView, ProfileView, CulinaryDNASection, DietaryPreferencesSection, RootView.swift)
  </files>
  <action>
Per locked decision: "Use String(localized:) initializer for all user-facing strings. Xcode auto-extracts into String Catalog."

Systematic approach — go through every view file and replace hardcoded strings:

**String replacement rules:**
1. `Text("Listen")` -> `Text(String(localized: "Listen"))`
2. `Button("Skip")` -> `Button(String(localized: "Skip"))`
3. `.accessibilityLabel("...")` -> `.accessibilityLabel(String(localized: "accessibility.view_name.element"))`
4. `.accessibilityHint("...")` -> `.accessibilityHint(String(localized: "accessibility.view_name.hint"))`
5. String interpolation: `"Photo of \(recipe.name)"` -> `String(localized: "Photo of \(recipe.name)")`
6. Tab labels: `Label("Feed", ...)` -> `Label(String(localized: "Feed"), ...)`

**Key convention for accessibility strings:** Use dotted prefix: `accessibility.{view}.{element}` (e.g., `accessibility.recipe_card.skip`, `accessibility.mini_player.play`).

**Key convention for UI strings:** Use natural English text as the key for simple strings (e.g., `"Listen"`, `"Skip"`, `"Bookmark"`). Use dotted keys for context-specific strings (e.g., `"onboarding.dietary.title"`, `"paywall.pro.description"`).

**Files to modify (in order):**

1. **FeedFeature views** (RecipeCardView, SwipeCardStack, FeedView, DietaryChipBar, DietaryChip, ViralBadge, ForYouBadge, EndOfStackCard, CardCountIndicator):
   - All button labels, badge text, chip text, accessibility labels/hints

2. **RecipeDetail views** (RecipeDetailView, IngredientChecklistView, StepTimelineView, ParallaxHeader):
   - Section headers, button labels, metadata labels, accessibility strings

3. **Location views** (LocationPickerView):
   - Search placeholder, "Use my location" text, headers

4. **VoicePlayback views** (MiniPlayerView, ExpandedPlayerView, VoicePickerView, VoiceUploadView):
   - Player labels, button text, picker headers, upload instructions

5. **Auth views** (OnboardingView, SignInStepView, DietaryPrefsStepView, LocationStepView, VoiceTeaserStepView, SignInGateView):
   - All onboarding text, button labels, accessibility hints

6. **Monetization views** (PaywallView, SubscriptionStatusView, AdCardView, BannerAdView):
   - Pricing text, tier descriptions, subscription status labels

7. **Profile views** (ProfileView, CulinaryDNASection, DietaryPreferencesSection):
   - Section headers, button labels, progress text

8. **RootView.swift**:
   - Tab labels: Feed, Me

**Important:** Do NOT localize:
- Developer-facing strings (Logger messages, debug strings)
- UserDefaults keys
- GraphQL query strings
- Bundle identifiers or technical constants
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && grep -rl 'String(localized:' Kindred/Packages/FeedFeature/Sources/ Kindred/Packages/VoicePlaybackFeature/Sources/ Kindred/Packages/AuthFeature/Sources/ Kindred/Packages/MonetizationFeature/Sources/ Kindred/Packages/ProfileFeature/Sources/ Kindred/Sources/App/RootView.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>
- Every view file uses String(localized:) for user-facing strings
- Accessibility labels and hints use String(localized:) with dotted convention
- Tab labels, button text, badge text, section headers all localized
- No hardcoded English user-facing strings remain in view files
- At least 25+ view files modified with String(localized:)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Localizable.xcstrings with Turkish translations and migrate remaining print() to Logger</name>
  <files>Kindred/Sources/Resources/Localizable.xcstrings, all reducer .swift files with remaining print() statements</files>
  <action>
**Localizable.xcstrings:**

Per locked decision: "One app-level String Catalog (.xcstrings) in the main app target — all strings extracted here."

1. Create `Kindred/Sources/Resources/Localizable.xcstrings` as a JSON file with the iOS 17+ String Catalog format.

2. Set `sourceLanguage: "en"` and include translations for `"en"` and `"tr"`.

3. Generate Turkish translations for ALL strings extracted in Task 1. Use these translation principles:
   - Keep translations concise (Turkish words average 30% longer than English)
   - Use informal "sen" form (not formal "siz") for a casual cooking app
   - Food/cooking terms: use commonly understood Turkish equivalents
   - UI verbs: "Listen" = "Dinle", "Skip" = "Atla", "Bookmark" = "Kaydet", "Search" = "Ara"

4. Key Turkish translations (common strings):
   - "Feed" = "Akis" (Flow)
   - "Me" = "Ben"
   - "Listen" = "Dinle"
   - "Skip" = "Atla"
   - "Bookmark" = "Kaydet"
   - "Search cities..." = "Sehir ara..."
   - "Use my location" = "Konumumu kullan"
   - "You're offline" = "Cevrimdisi"
   - "Back online" = "Tekrar cevrimici"
   - "Voice narration requires internet" = "Sesli anlatim icin internet gerekli"
   - "Pro" = "Pro"
   - "Free" = "Ucretsiz"
   - "Bookmarks" = "Kaydedilenler"
   - "Dietary Preferences" = "Beslenme Tercihleri"
   - "Culinary DNA" = "Mutfak DNA'si"
   - "Now playing" = "Simdi caliyor"
   - "Paused" = "Durduruldu"
   - "Photo of" = "Fotografi:"
   - "minutes" = "dakika"
   - "calories" = "kalori"
   - "Viral" = "Viral"

5. Include plurals where needed (e.g., bookmark count):
   ```json
   "bookmarks.count %lld": {
       "localizations": {
           "en": { "variations": { "plural": { "zero": { "stringUnit": { "value": "No bookmarks" } }, "one": { "stringUnit": { "value": "1 bookmark" } }, "other": { "stringUnit": { "value": "%lld bookmarks" } } } } },
           "tr": { "variations": { "plural": { "zero": { "stringUnit": { "value": "Kayit yok" } }, "one": { "stringUnit": { "value": "1 kayit" } }, "other": { "stringUnit": { "value": "%lld kayit" } } } } }
       }
   }
   ```

**Logger migration (remaining reducers):**

1. Check ALL reducer files for remaining `print()` statements:
   - `VoicePlaybackReducer.swift` (if not done in Plan 03)
   - `AudioPlayerManager.swift`
   - `VoiceCacheClient.swift`
   - `OnboardingReducer.swift`
   - `SignInGateReducer.swift`
   - `SignInClient.swift`
   - `SubscriptionReducer.swift`
   - `SubscriptionClient.swift`
   - `AdClient.swift`
   - `ProfileReducer.swift`
   - `PersonalizationClient.swift`
   - `GuestSessionClient.swift`
   - `LocationClient.swift`
   - `LocationManager.swift`
   - `NetworkMonitorClient.swift`
   - `GuestMigrationClient.swift`

2. For each file with print() statements:
   - Add `import OSLog` if not present
   - Add appropriate Logger extension if not already defined:
     ```swift
     extension Logger {
         private static var subsystem = Bundle.main.bundleIdentifier!
         static let {category} = Logger(subsystem: subsystem, category: "{category-name}")
     }
     ```
   - Replace `print("...")` with `Logger.{category}.{level}("...")`:
     - Success messages -> `.info` or `.notice`
     - Error messages -> `.error`
     - Debug/verbose -> `.debug`
     - Warnings -> `.warning`
   - Apply privacy annotations: `.private` for user IDs, recipe IDs, tokens; `.public` for error codes, state names

3. Logger category assignments:
   - VoicePlayback/Audio: `voice-playback`, `audio-cache`
   - Auth: `auth`, `onboarding`
   - Monetization: `subscription`, `ads`
   - Profile: `profile`
   - Feed: `feed`, `personalization`, `location`
   - Network: `network`
   - Guest: `guest-session`
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && test -f Kindred/Sources/Resources/Localizable.xcstrings && echo "xcstrings exists" && grep -rl 'print(' Kindred/Packages/*/Sources/**/*.swift Kindred/Sources/App/*.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>
- Localizable.xcstrings exists at Kindred/Sources/Resources/ with sourceLanguage "en"
- Turkish translations exist for all extracted strings
- Plurals handled for count-based strings (bookmarks, etc.)
- Zero print() statements remain across all feature packages and app sources
- Logger extensions defined for every feature category
- Privacy annotations applied to sensitive data in log messages
  </done>
</task>

</tasks>

<verification>
1. Localizable.xcstrings exists and is valid JSON with "en" and "tr" localizations
2. At least 25+ view files use String(localized:)
3. Zero print() statements in Kindred/Packages/ and Kindred/Sources/App/
4. Logger extensions present in feature packages
5. Turkish translations are reasonable (spot-check: "Listen" = "Dinle", "Skip" = "Atla")
6. Project builds
</verification>

<success_criteria>
- App displays Turkish text when iOS language is set to Turkish
- All user-facing strings are localized via String Catalog
- All developer logging uses os.log Logger with proper categories and privacy
- No print() statements remain anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/10-accessibility-polish/10-05-SUMMARY.md`
</output>
