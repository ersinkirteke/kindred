---
phase: 10-accessibility-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/SwipeCardStack.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/DietaryChipBar.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/MiniPlayerView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePlaybackReducer.swift
  - Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift
  - Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift
  - Kindred/Packages/FeedFeature/Sources/Location/LocationPickerView.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
autonomous: true
requirements:
  - ACCS-05

must_haves:
  truths:
    - "VoiceOver navigation works correctly on all screens with meaningful labels, hints, and reading order"
    - "Card swipes become fade-out/fade-in when Reduce Motion is enabled"
    - "Hero transitions become crossfade when Reduce Motion is enabled"
    - "MiniPlayerView is a single combined VoiceOver element with custom actions (play/pause, expand, dismiss)"
    - "Playback state changes announce via VoiceOver ('Now playing...', 'Paused')"
    - "Location pill uses Button semantics (not .onTapGesture) for VoiceOver/keyboard"
    - "Badges (Viral, ForYou) are included in combined card accessibility label, not separate elements"
    - "Tab bar has dynamic badge count in accessibility value"
  artifacts:
    - path: "Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift"
      provides: "Reduce Motion transitions, combined VoiceOver labels with badges"
    - path: "Kindred/Packages/VoicePlaybackFeature/Sources/Player/MiniPlayerView.swift"
      provides: "Combined VoiceOver element with custom actions"
    - path: "Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePlaybackReducer.swift"
      provides: "VoiceOver announcements for playback state changes"
  key_links:
    - from: "VoicePlaybackReducer.swift"
      to: "UIAccessibility"
      via: "UIAccessibility.post(notification: .announcement) on playback state change"
      pattern: "UIAccessibility\\.post"
---

<objective>
Comprehensive VoiceOver polish and Reduce Motion implementation across all app screens. Every interactive element gets proper accessibility labels, hints, and combined elements. All animations get Reduce Motion fallbacks.

Purpose: Ensures VoiceOver users can navigate every screen with clear, meaningful labels and proper reading order. Reduce Motion users experience simplified animations instead of complex springs/zooms.

Output: Updated view files across Feed, VoicePlayback, Auth, Monetization, and Profile features with VoiceOver and Reduce Motion support.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-accessibility-polish/10-CONTEXT.md
@.planning/phases/10-accessibility-polish/10-RESEARCH.md

# Plan 01 SUMMARY needed for Typography @ScaledMetric pattern
@.planning/phases/10-accessibility-polish/10-01-SUMMARY.md

@Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift
@Kindred/Packages/FeedFeature/Sources/Feed/SwipeCardStack.swift
@Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift
@Kindred/Packages/VoicePlaybackFeature/Sources/Player/MiniPlayerView.swift
@Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift
@Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePlaybackReducer.swift
@Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift
@Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift
@Kindred/Packages/FeedFeature/Sources/Location/LocationPickerView.swift
@Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoiceOver polish for Feed, RecipeDetail, and Location views</name>
  <files>Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift, Kindred/Packages/FeedFeature/Sources/Feed/SwipeCardStack.swift, Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift, Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift, Kindred/Packages/FeedFeature/Sources/Feed/DietaryChipBar.swift, Kindred/Packages/FeedFeature/Sources/Location/LocationPickerView.swift</files>
  <action>
**RecipeCardView.swift:**
1. Ensure Viral and ForYou badges are included in the combined card accessibility label (not separate VoiceOver elements). Per locked decision: "Badges (Viral, ForYou): included in combined card accessibility label, not separate elements." The card's `.accessibilityLabel` computed property should append "Viral recipe" and/or "Personalized for you" as appropriate (verify existing implementation already does this from Phase 5, fix if not).
2. Add `@Environment(\.accessibilityReduceMotion) var reduceMotion`.
3. Replace spring animations with linear for Reduce Motion: card offset/rotation animation should use `.linear(duration: 0.2)` when reduceMotion is true, spring when false. Per locked decision: "Card swipes become fade-out/fade-in."
4. Replace card transitions: use `.opacity` when reduceMotion is true, `.asymmetric(insertion: .move(edge: .trailing).combined(with: .opacity), removal: .move(edge: .leading).combined(with: .opacity))` when false.
5. Recipe images: set `.accessibilityLabel("Photo of \(recipe.name)")` on hero images. Per locked decision: "Recipe images: use recipe name as alt text."

**SwipeCardStack.swift:**
1. Add `@Environment(\.accessibilityReduceMotion) var reduceMotion`.
2. Ensure card swipe animations respect Reduce Motion setting (pass through to RecipeCardView if needed).
3. Per locked decision "Single card mode at AX1+": Add `@Environment(\.dynamicTypeSize) var dynamicTypeSize`. When `dynamicTypeSize.isAccessibilitySize`, hide peeking cards behind the top card (only show top card). This means the card stack should only render 1 card at AX sizes, not 2-3.

**FeedView.swift:**
1. Ensure VoiceOver announces filter changes. When dietary filter is toggled, add announcement: `UIAccessibility.post(notification: .announcement, argument: "Showing \(filterName) recipes")` or "Showing all recipes" when filter cleared.
2. VoiceOver announces connectivity changes if not already handled at app level (check if FeedReducer already does this; if so, skip to avoid duplicate announcements).

**RecipeDetailView.swift:**
1. Set reading order per locked decision: "Recipe detail reading order: top-down natural (name -> image -> metadata -> ingredients -> steps -> voice player)." Use `.accessibilitySortPriority()` if the natural order doesn't match, or verify the VStack layout already provides correct order.
2. Hero image: `.accessibilityLabel("Photo of \(recipe.name)")`.
3. If hero transition uses `.matchedTransitionSource` or `.zoom`: add `@Environment(\.accessibilityReduceMotion) var reduceMotion` and swap to `.crossfade` under Reduce Motion. Per locked decision: "Hero transition under Reduce Motion: crossfade instead of .zoom."

**DietaryChipBar.swift:**
1. Add `@Environment(\.dynamicTypeSize) var dynamicTypeSize`.
2. Per locked decision: "DietaryChipBar: wrap to multiple rows at AX sizes (vertical flow layout instead of horizontal scroll)." When `dynamicTypeSize.isAccessibilitySize`, switch from horizontal ScrollView to a wrapping layout (use SwiftUI's `FlowLayout` or a simple `LazyVGrid` with flexible columns to wrap chips to multiple rows).
3. Each chip: `.accessibilityLabel("\(chipName) filter")` + `.accessibilityHint("Double tap to toggle")`.

**LocationPickerView.swift:**
1. Per locked decision: "Location pill: convert from .onTapGesture to proper Button for VoiceOver and keyboard semantics." Find the location pill (city badge at top of feed that opens the picker) — if it uses `.onTapGesture`, wrap the content in a `Button` instead. This ensures VoiceOver announces it as a button and it's keyboard-accessible.
2. Add `.accessibilityLabel("Location: \(cityName)")` and `.accessibilityHint("Double tap to change location")`.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && grep -c "reduceMotion\|accessibilityReduceMotion" Kindred/Packages/FeedFeature/Sources/Feed/RecipeCardView.swift && grep -c "isAccessibilitySize\|dynamicTypeSize" Kindred/Packages/FeedFeature/Sources/Feed/DietaryChipBar.swift && grep -c "Button\|accessibilityLabel\|accessibilityHint" Kindred/Packages/FeedFeature/Sources/Location/LocationPickerView.swift</automated>
  </verify>
  <done>
- RecipeCardView: Reduce Motion transitions (opacity vs spring), badges in combined label, image alt text
- SwipeCardStack: Single card mode at AX sizes
- FeedView: VoiceOver announcement for filter changes
- RecipeDetailView: Correct reading order, hero image alt text, Reduce Motion crossfade
- DietaryChipBar: Wraps to rows at AX sizes
- LocationPickerView: Button semantics (not onTapGesture), accessibility label and hint
  </done>
</task>

<task type="auto">
  <name>Task 2: VoiceOver polish for VoicePlayback, Profile, Onboarding, and Paywall</name>
  <files>Kindred/Packages/VoicePlaybackFeature/Sources/Player/MiniPlayerView.swift, Kindred/Packages/VoicePlaybackFeature/Sources/Player/ExpandedPlayerView.swift, Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePlaybackReducer.swift, Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift, Kindred/Packages/MonetizationFeature/Sources/Subscription/PaywallView.swift, Kindred/Packages/ProfileFeature/Sources/ProfileView.swift</files>
  <action>
**MiniPlayerView.swift:**
1. Per locked decision: "MiniPlayerView: single combined VoiceOver element with custom actions (play/pause, expand, dismiss)."
2. Wrap entire MiniPlayerView content in `.accessibilityElement(children: .combine)`.
3. Set `.accessibilityLabel` to include recipe name and voice name: "Now playing \(recipeName) by \(voiceName)" or "Paused: \(recipeName) by \(voiceName)".
4. Add `.accessibilityAction(named: "Play")` / `.accessibilityAction(named: "Pause")` (toggle based on state).
5. Add `.accessibilityAction(named: "Expand player")` for full player view.
6. Add `.accessibilityAction(named: "Dismiss")` to stop playback.

**ExpandedPlayerView.swift:**
1. Add `@Environment(\.dynamicTypeSize) var dynamicTypeSize`.
2. Per locked decision: "ExpandedPlayerView controls: stack vertically at AX sizes." When `dynamicTypeSize.isAccessibilitySize`, change the play/pause/skip button row from HStack to VStack.
3. Ensure play button has `.accessibilityLabel("Play")` or `.accessibilityLabel("Pause")` based on state.
4. Seek slider: `.accessibilityLabel("Playback position")` + `.accessibilityValue("\(currentTime) of \(duration)")`.

**VoicePlaybackReducer.swift:**
1. Per locked decision: "Announce all playback state changes."
2. In the case that handles status/state changes (look for `.statusChanged` or similar):
   - `.playing`: `UIAccessibility.post(notification: .announcement, argument: "Now playing: \(recipeName) by \(voiceName)")`
   - `.paused`: `UIAccessibility.post(notification: .announcement, argument: "Paused")`
   - `.error`: `UIAccessibility.post(notification: .announcement, argument: "Playback error")`
3. Add `import UIKit` if not already present (needed for UIAccessibility).
4. Replace any `print()` statements with `Logger.voicePlayback` calls. Add Logger extension:
   ```swift
   import OSLog
   extension Logger {
       private static var subsystem = Bundle.main.bundleIdentifier!
       static let voicePlayback = Logger(subsystem: subsystem, category: "voice-playback")
   }
   ```

**OnboardingView.swift:**
1. Per locked decision: "Full onboarding step descriptions: each step has .accessibilityHint explaining purpose."
2. Add `.accessibilityHint` to each onboarding step view:
   - Sign-in step: "Sign in to save your preferences and voice profiles"
   - Dietary step: "Select dietary preferences to filter recipes"
   - Location step: "Set your location to see recipes trending near you"
   - Voice teaser step: "Upload your voice to hear recipes narrated in your voice"
3. Per locked decision: "Onboarding steps: add scroll-for-more indicators when content overflows at AX sizes." Add `@Environment(\.dynamicTypeSize) var dynamicTypeSize`. When `dynamicTypeSize.isAccessibilitySize`, wrap each step's content in ScrollView and add a gradient fade indicator at the bottom (a subtle gradient from clear to kindredBackground) to suggest scrollability.

**PaywallView.swift:**
1. Add `@Environment(\.dynamicTypeSize) var dynamicTypeSize`.
2. Per locked decision: "PaywallView: stack subscription tiers vertically at AX sizes." When `dynamicTypeSize.isAccessibilitySize`, switch tier comparison from side-by-side HStack to stacked VStack.
3. VoiceOver labels for pricing: "Pro plan, $9.99 per month, removes ads and unlimited voice slots".

**ProfileView.swift:**
1. Tab bar: Per locked decision "Tab bar: labels + dynamic badge count in accessibility value." On the Me tab badge in RootView (handled here for the ProfileView content context), ensure the badge count is conveyed. This is actually in RootView.swift — add `.accessibilityValue(store.feedState.bookmarkCount > 0 ? "\(store.feedState.bookmarkCount) saved recipes" : "")` to the Me tab item. NOTE: If this is easier to do in RootView, modify RootView instead. Add to the `.tabItem` modifier area.
2. Profile sections: ensure VoiceOver reads subscription status, culinary DNA progress, dietary preferences with clear labels.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && grep -c "accessibilityElement\|accessibilityAction\|accessibilityLabel" Kindred/Packages/VoicePlaybackFeature/Sources/Player/MiniPlayerView.swift && grep -c "UIAccessibility.post\|announcement" Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePlaybackReducer.swift && grep -c "accessibilityHint\|dynamicTypeSize" Kindred/Packages/AuthFeature/Sources/Onboarding/OnboardingView.swift</automated>
  </verify>
  <done>
- MiniPlayerView: combined VoiceOver element with play/pause, expand, dismiss custom actions
- ExpandedPlayerView: vertical layout at AX sizes, labeled controls
- VoicePlaybackReducer: VoiceOver announcements for playing/paused/error states, Logger replaces print()
- OnboardingView: accessibility hints on all steps, scroll indicators at AX sizes
- PaywallView: vertical tiers at AX sizes, clear pricing labels
- ProfileView/RootView: tab badge accessibility value with saved recipe count
  </done>
</task>

</tasks>

<verification>
1. RecipeCardView: `@Environment(\.accessibilityReduceMotion)` present, transitions conditional
2. MiniPlayerView: `.accessibilityElement(children: .combine)` and 3+ custom actions
3. VoicePlaybackReducer: `UIAccessibility.post` for playing and paused states
4. DietaryChipBar: `dynamicTypeSize.isAccessibilitySize` check with wrapping layout
5. OnboardingView: `.accessibilityHint` on each step view
6. PaywallView: VStack at AX sizes for tiers
7. LocationPickerView: Button wrapper instead of .onTapGesture
8. Project builds
</verification>

<success_criteria>
- VoiceOver users can navigate every screen with clear, meaningful labels
- All playback state changes are announced via VoiceOver
- Reduce Motion enabled: card swipes fade, hero transitions crossfade, springs become linear
- AX sizes: single card mode, wrapping chip bar, vertical player controls, vertical paywall tiers
- Location pill is a proper Button for VoiceOver and keyboard navigation
</success_criteria>

<output>
After completion, create `.planning/phases/10-accessibility-polish/10-03-SUMMARY.md`
</output>
