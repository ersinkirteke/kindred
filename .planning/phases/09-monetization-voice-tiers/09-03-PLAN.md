---
phase: 09-monetization-voice-tiers
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift
  - Kindred/Packages/FeedFeature/Sources/Feed/SwipeCardStack.swift
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift
  - Kindred/Packages/FeedFeature/Package.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePickerView.swift
  - Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePlaybackReducer.swift
  - Kindred/Packages/VoicePlaybackFeature/Package.swift
autonomous: true
requirements:
  - MONET-01
  - MONET-02
  - VOICE-07
must_haves:
  truths:
    - "Free user sees native ad cards every 5 recipe cards in feed swipe stack"
    - "Ad cards are swipeable left to dismiss like recipe cards"
    - "Recipe detail shows banner ad below ingredients, above step timeline"
    - "Banner ad hides when voice narration is active"
    - "No ads shown on very first app launch ever"
    - "Pro users see zero ads anywhere"
    - "Free user with 1 voice profile sees 'Upgrade to Pro' CTA instead of 'Create Voice Profile' button"
    - "Free user hitting voice limit triggers paywall bottom sheet"
  artifacts:
    - path: "Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift"
      provides: "Ad card injection logic every 5 recipes"
      contains: "adCard"
    - path: "Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift"
      provides: "Banner ad between ingredients and steps"
      contains: "BannerAdView"
    - path: "Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePickerView.swift"
      provides: "Voice slot limit check and upgrade CTA"
      contains: "Upgrade to Pro"
  key_links:
    - from: "FeedReducer"
      to: "SubscriptionClient"
      via: "@Dependency for entitlement check"
      pattern: "subscriptionClient"
    - from: "FeedView/SwipeCardStack"
      to: "AdCardView"
      via: "Interleaved in card array"
      pattern: "AdCardView"
    - from: "RecipeDetailView"
      to: "BannerAdView"
      via: "Conditional insertion"
      pattern: "BannerAdView"
    - from: "VoicePickerView"
      to: "PaywallView"
      via: "Voice slot limit trigger"
      pattern: "showPaywall"
---

<objective>
Wire ad components and voice slot enforcement into existing features: inject AdCardView every 5 recipes in the feed, add BannerAdView to recipe detail (hidden during narration), enforce voice slot limits in VoicePickerView with paywall trigger, and gate all ads behind subscription status checks.

Purpose: This is the integration plan that connects Plan 01 (subscription) and Plan 02 (ads) into the existing FeedFeature and VoicePlaybackFeature.
Output: Ads visible for free users, hidden for Pro. Voice slot limit triggers paywall at point of need.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-monetization-voice-tiers/09-CONTEXT.md
@.planning/phases/09-monetization-voice-tiers/09-RESEARCH.md
@.planning/phases/09-monetization-voice-tiers/09-01-SUMMARY.md
@.planning/phases/09-monetization-voice-tiers/09-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From MonetizationFeature/Sources/Subscription/SubscriptionClient.swift:
```swift
@DependencyClient
public struct SubscriptionClient: Sendable {
    public var loadProducts: @Sendable () async throws -> [Product]
    public var purchase: @Sendable (Product) async throws -> StoreKit.Transaction
    public var restorePurchases: @Sendable () async throws -> Void
    public var currentEntitlement: @Sendable () async -> SubscriptionStatus
    public var observeTransactionUpdates: @Sendable () async -> AsyncStream<StoreKit.Transaction>
    public var jwsRepresentation: @Sendable (StoreKit.Transaction) async -> String?
}
```

From MonetizationFeature/Sources/Models/SubscriptionModels.swift:
```swift
public enum SubscriptionStatus: Equatable, Sendable {
    case unknown, free, pro(expiresDate: Date, isInGracePeriod: Bool)
}
```

<!-- From Plan 02 outputs -->
From MonetizationFeature/Sources/Ads/AdClient.swift:
```swift
@DependencyClient
public struct AdClient: Sendable {
    public var initializeSDK: @Sendable () async -> Void
    public var shouldShowAds: @Sendable () -> Bool
    public var isFirstLaunchEver: @Sendable () -> Bool
}
```

From MonetizationFeature/Sources/Ads/AdCardView.swift:
```swift
public struct AdCardView: View { /* onUpgradeTapped callback */ }
```

From MonetizationFeature/Sources/Ads/BannerAdView.swift:
```swift
public struct BannerAdView: View { /* adaptive banner */ }
```

<!-- Existing code to modify -->
From FeedReducer.swift:
- State has: cardStack: [RecipeCard], currentAuthState: AuthState
- Actions already handle swipeCard, recipesLoaded, etc.

From SwipeCardStack.swift:
- Takes `cards: [RecipeCard]`, renders top 2

From RecipeDetailView.swift:
- Has recipeContentView with IngredientChecklistView then StepTimelineView
- PlaybackObserver.shared for narration active check

From VoicePickerView.swift:
- Has "Create Voice Profile" button both in empty state and at bottom of voice list
- Takes voiceProfiles array, onCreateProfile callback

From VoicePlaybackReducer.swift:
- State has: voiceProfiles: [VoiceProfile], showVoicePicker: Bool
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject ad cards into feed and add banner to recipe detail</name>
  <files>
    Kindred/Packages/FeedFeature/Package.swift,
    Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift,
    Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift,
    Kindred/Packages/FeedFeature/Sources/Feed/SwipeCardStack.swift,
    Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift
  </files>
  <action>
    Update FeedFeature/Package.swift:
    - Add dependency: `.package(name: "MonetizationFeature", path: "../MonetizationFeature")`
    - Add to target: `"MonetizationFeature"`

    Update FeedReducer.swift State:
    - Add: `public var subscriptionStatus: SubscriptionStatus = .unknown`
    - Add: `public var shouldShowAds: Bool = false` (computed from subscriptionStatus + first-launch check)
    - Add dependency: `@Dependency(\.subscriptionClient) var subscriptionClient`
    - Add dependency: `@Dependency(\.adClient) var adClient`

    Update FeedReducer.swift Actions:
    - Add: `case subscriptionStatusUpdated(SubscriptionStatus)`
    - Add: `case adVisibilityDetermined(Bool)` — result of shouldShowAds check

    Update FeedReducer.swift body (onAppear effect):
    - After existing effects, add: check subscription status and ad visibility
    - In `.subscriptionStatusUpdated`: update `state.subscriptionStatus`, compute `state.shouldShowAds` (status is .free AND adClient.shouldShowAds() returns true)
    - In `.adVisibilityDetermined`: set state.shouldShowAds

    Feed ad interleaving approach — do NOT modify cardStack array (RecipeCard type).
    Instead, handle in FeedView/SwipeCardStack:

    Update SwipeCardStack.swift:
    - Add parameter: `showAds: Bool = false`
    - Add parameter: `onAdUpgradeTapped: () -> Void = {}`
    - Add computed property: card index tracking. When rendering, if `showAds` is true and the card at the current position is a multiple of 5 (after every 5th recipe card swiped), show AdCardView instead of the next RecipeCard
    - SIMPLER APPROACH: Add `adCardInterval: Int = 5` parameter. Track total cards shown. After every `adCardInterval` recipe cards, overlay an AdCardView on top before showing the next recipe card.
    - Actually, SIMPLEST: Add `adEveryN: Int? = nil` parameter. In SwipeCardStack, maintain `@State var swipeCount: Int = 0`. On each swipe, increment count. When `swipeCount % adEveryN == 0 && swipeCount > 0`, show an AdCardView overlay instead of the recipe card. The AdCardView is also swipeable left to dismiss (same gesture).

    Update FeedView.swift:
    - Import MonetizationFeature
    - Pass `adEveryN: store.shouldShowAds ? 5 : nil` to SwipeCardStack
    - Pass `onAdUpgradeTapped: { store.send(.showPaywall) }` (or delegate action to parent for paywall presentation)

    Update RecipeDetailView.swift:
    - Import MonetizationFeature
    - In recipeContentView, between IngredientChecklistView and "Instructions" sectionHeader + StepTimelineView:
      - Add conditional: `if shouldShowBannerAd { BannerAdView(...) }`
      - `shouldShowBannerAd`: computed from subscription status (free, not first launch) AND narration NOT active (effectivePlaybackStatus != .playing && effectivePlaybackStatus != .loading && effectivePlaybackStatus != .buffering)
    - Per user decision: banner hides when voice narration is active
    - Add `subscriptionStatus` to RecipeDetailReducer.State (or pass through from parent)
    - If adding state is too heavy, use a simple `showBannerAd: Bool` parameter passed from parent or environment

    Add `import MonetizationFeature` to all modified files.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && swift build --package-path Kindred/Packages/FeedFeature 2>&1 | tail -10</automated>
  </verify>
  <done>Free users see ad cards every 5 recipes in swipe stack (swipeable left to dismiss). Recipe detail shows adaptive banner below ingredients, hidden during voice narration. Pro users and first-launch users see zero ads.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce voice slot limits and wire paywall trigger in VoicePickerView</name>
  <files>
    Kindred/Packages/VoicePlaybackFeature/Package.swift,
    Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePickerView.swift,
    Kindred/Packages/VoicePlaybackFeature/Sources/Player/VoicePlaybackReducer.swift
  </files>
  <action>
    Update VoicePlaybackFeature/Package.swift:
    - Add dependency: `.package(name: "MonetizationFeature", path: "../MonetizationFeature")`
    - Add to target: `"MonetizationFeature"`

    Update VoicePickerView.swift per user decisions:
    - Add new parameter: `subscriptionStatus: SubscriptionStatus` (from MonetizationFeature)
    - Add new parameter: `onUpgradeTapped: () -> Void`
    - Computed property: `var isAtVoiceLimit: Bool` — true when subscriptionStatus is .free (not .pro) AND voiceProfiles.count >= 1
    - When `isAtVoiceLimit` is true:
      - Replace the "Create Voice Profile" button (the dashed-border button at bottom of voice list) with an "Upgrade to Pro for more voices" CTA:
        - HStack with crown icon (SF Symbol: "crown.fill"), text "Upgrade to Pro for more voices"
        - Styled with .kindredAccent color, same padding as original button
        - Tapping calls `onUpgradeTapped()`
      - In empty state (no profiles), keep the "Create Voice Profile" button as-is (free user can still create their first voice)
    - When NOT at voice limit (Pro users or free users with 0 voices):
      - Keep existing "Create Voice Profile" button unchanged
    - Accessibility: upgrade CTA labeled "Upgrade to Kindred Pro for unlimited voice profiles"

    Update VoicePlaybackReducer.swift:
    - Add to State: `public var subscriptionStatus: SubscriptionStatus = .unknown`
    - Add dependency: `@Dependency(\.subscriptionClient) var subscriptionClient`
    - Add Actions:
      - `case checkSubscriptionStatus`
      - `case subscriptionStatusUpdated(SubscriptionStatus)`
      - `case upgradeTapped` — sets `showPaywall = true` (reuse showVoicePicker pattern or add new `showPaywall: Bool` to state)
    - In `onAppear` or voice picker presentation, check subscription status
    - When free user taps "Create Voice Profile" AND already has 1 voice → send `upgradeTapped` instead of `createVoiceProfile`
    - Add `public var showPaywall: Bool = false` to State
    - Add `case showPaywall` and `case dismissPaywall` actions

    Wire PaywallView presentation:
    - In the parent view that presents VoicePickerView (likely in ExpandedPlayerView or wherever VoicePickerView is shown as sheet), add a `.sheet(isPresented:)` for PaywallView when `showPaywall` is true
    - PaywallView takes a SubscriptionReducer store — either scope from parent or create inline

    Per user decisions:
    - Downgrade handling: users who cancel Pro keep ALL existing voices usable, just can't create new ones. This is handled naturally — voice slot check only applies to CREATION, not usage.
  </action>
  <verify>
    <automated>cd /Users/ersinkirteke/Workspaces/Kindred && swift build --package-path Kindred/Packages/VoicePlaybackFeature 2>&1 | tail -10</automated>
  </verify>
  <done>Free user with 1 voice sees "Upgrade to Pro for more voices" CTA replacing "Create Voice Profile". Tapping CTA triggers PaywallView bottom sheet. Pro users see normal "Create Voice Profile" button. Downgraded users keep existing voices but cannot create new ones.</done>
</task>

</tasks>

<verification>
1. FeedFeature compiles with MonetizationFeature dependency
2. Ad cards appear every 5 recipe cards in feed (free users only)
3. Ad cards swipeable left to dismiss
4. Recipe detail banner visible between ingredients and instructions
5. Banner hides during voice narration
6. No ads on first app launch ever
7. Pro users see zero ads
8. VoicePlaybackFeature compiles with MonetizationFeature dependency
9. Free user at voice limit sees upgrade CTA in VoicePickerView
10. Upgrade CTA triggers paywall bottom sheet
</verification>

<success_criteria>
- Free tier ad experience matches user decisions exactly (every 5 cards, recipe detail banner, swipeable, "Sponsored" label, upsell link)
- Pro tier is ad-free
- Voice slot enforcement blocks creation at 1 voice for free users
- Paywall appears contextually at voice limit (not randomly)
- Narration-active banner suppression works
</success_criteria>

<output>
After completion, create `.planning/phases/09-monetization-voice-tiers/09-03-SUMMARY.md`
</output>
