---
phase: 09-monetization-voice-tiers
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-07T17:41:19.714Z
---

<current_state>
Phase 9 execution: Plans 01-04 fully executed (Wave 1 + Wave 2 complete). Plan 05 (device verification checkpoint) is in progress — Task 1 done (build fixes), Task 2 (human verification) in progress.

The app is installed on device and partially working. User has confirmed ads appear after 5 swipes but found issues during testing. We're fixing bugs found during the checkpoint verification.
</current_state>

<completed_work>

## Plans Executed
- Plan 09-01: MonetizationFeature package — SubscriptionClient, SubscriptionReducer, PaywallView, SubscriptionStatusView, StoreKit config (4 commits: 34926fb, 49185ef, f9118ba, a71dfce)
- Plan 09-02: AdMob integration — AdClient, AdCardView, BannerAdView (3 commits: 8225fb7, 6f9c337, a669405)
- Plan 09-03: Feed ad injection, recipe detail banner, voice slot enforcement (3 commits: 8a68ba5, d5bb89c, 34d5bdf)
- Plan 09-04: Profile subscription UI, AppDelegate lifecycle, backend JWS verification (4 commits: 284fef4, 030acc5, f991678, 07c708b)
- Plan 09-05 Task 1: Build fixes for device deployment (commit: 19c08bf)

## Build Fixes Applied (not committed yet)
1. **Bundle ID**: project.yml had `com.kindred.app` (wrong) → restored `com.ersinkirteke.kindred`
2. **Stale caches**: Removed `.build` and `.swiftpm` directories from all local packages
3. **GoogleMobileAds missing from MonetizationFeature Package.swift**: Added `swift-package-manager-google-mobile-ads` dependency + `GoogleMobileAds` product
4. **GADApplicationIdentifier missing from Info.plist**: Added test app ID `ca-app-pub-3940256099942544~1458002511` + SKAdNetworkItems
5. **App crash on launch**: Caused by missing GADApplicationIdentifier — fixed above
6. **AdCardView ads not loading**: Three fixes:
   - `GADAdLoader` stored as `@State` (was local var, deallocated before callback)
   - `rootViewController` provided (was nil)
   - `GADNativeAdLoaderDelegate` protocol (was using wrong `GADNativeAdDelegate`)
7. **Ads not showing after 5 swipes**: Race condition — `adVisibilityDetermined` fired before `subscriptionStatusUpdated`, so status was `.unknown` not `.free`. Fixed by treating `.unknown` same as `.free` (only `.pro` suppresses ads).
8. **Test device ID**: Added `GADSimulatorID` to requestConfiguration (note: may need real device ID for physical device test ads)
</completed_work>

<remaining_work>

## Current Bug — "Remove ads with Pro" button not working
- `FeedReducer.showPaywall` action is a TODO stub that returns `.none`
- Need to add `isPaywallPresented` state to FeedReducer
- Wire PaywallView (from MonetizationFeature) as a `.sheet` in FeedView
- The PaywallView already exists in MonetizationFeature/Sources/Subscription/PaywallView.swift

## Plan 09-05 Task 2 Verification Checklist (remaining items to test)
- [ ] Subscription purchase flow (steps 8-12)
- [ ] Pro tier ad suppression (steps 13-15)
- [ ] Voice slot enforcement (step 16)
- [ ] Persistence after relaunch (steps 17-18)
- [ ] First-launch ad suppression needs testing (AppDelegate sets flag in didFinishLaunchingWithOptions BEFORE views check — may show ads on first launch)

## After Verification
- Commit all build/bug fixes
- Create 09-05 SUMMARY.md
- Run gsd-verifier for phase goal verification
- Update ROADMAP.md with phase completion
- Present final status
</remaining_work>

<decisions_made>

- Used Google's test ad unit IDs (ca-app-pub-3940256099942544/*) for development
- Used `GADSimulatorID` for test device registration — real device may need actual device advertising ID
- Treated `.unknown` subscription status same as `.free` for ad visibility (only `.pro` suppresses)
- Kept command-line builds with `-skipMacroValidation` flag for Xcode 26 compatibility
- MonetizationFeature Package.swift needs GoogleMobileAds as explicit dependency (executors missed this)
</decisions_made>

<blockers>
- "Remove ads with Pro" button in AdCardView → FeedReducer.showPaywall is TODO stub, needs PaywallView sheet wiring
- First-launch ad suppression may be broken: AppDelegate sets `kindredFirstLaunchComplete` flag synchronously in didFinishLaunchingWithOptions, so by the time views check `shouldShowAds()`, flag already exists → ads show on first launch too
- Subscribe button in Profile "Upgrade to Kindred Pro" ($9.99/month) not working — `subscribeTapped` calls `subscriptionClient.purchase(product)` but product list may be empty (no StoreKit Testing configuration active on device install vs Xcode run), OR the SubscriptionClient.purchase liveValue may not be implemented. Check: (1) is `state.subscriptionProducts` populated? (2) does `subscriptionClient.loadProducts()` actually call `Product.products(for:)` with correct product ID "com.kindred.pro.monthly"? (3) StoreKit Testing only works when launched FROM Xcode with the storekit config — command-line install won't have it. User must build & run from Xcode (Cmd+R) for StoreKit Testing to work.
- Banner ad NOT visible in recipe detail view — BannerAdView is at line 162 of RecipeDetailView.swift (between ingredients and instructions), but user doesn't see it. Likely same issues as AdCardView: wrong delegate protocol, loader lifetime, or `shouldShowBannerAd` is false. Check RecipeDetailView's `shouldShowBannerAd` computed property and BannerAdView's ad loading logic
</blockers>

<context>
We're in the execute-phase workflow, specifically at the Wave 3 checkpoint (Plan 09-05 human verification). The user is testing on their iPhone 16 Pro Max. Waves 1-2 executed successfully with parallel agents. Multiple build issues were found and fixed during device testing — mostly integration gaps the executor agents missed (missing Info.plist keys, wrong protocols, race conditions, Package.swift dependencies).

The verification is partially done — ads load and appear after 5 swipes. Main remaining work is wiring the paywall presentation and testing the subscription purchase flow.

Key files modified but not yet committed:
- Kindred/project.yml (bundle ID fix)
- Kindred/Sources/Info.plist (GADApplicationIdentifier)
- Kindred/Sources/App/AppDelegate.swift (test device ID)
- Kindred/Packages/MonetizationFeature/Package.swift (GoogleMobileAds dependency)
- Kindred/Packages/MonetizationFeature/Sources/Ads/AdCardView.swift (loader lifetime, protocol, rootVC)
- Kindred/Packages/FeedFeature/Sources/Feed/FeedReducer.swift (race condition fix)
</context>

<next_action>
1. Wire PaywallView presentation: Add `@Presents var paywall: SubscriptionReducer.State?` to FeedReducer.State, handle `.showPaywall` by setting paywall state, add `.sheet` to FeedView
2. Continue device verification checklist (steps 8-18)
3. Commit all accumulated fixes
4. Complete Plan 09-05 SUMMARY.md
5. Run phase verification and update roadmap
</next_action>
