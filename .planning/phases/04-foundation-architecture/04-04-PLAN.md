---
phase: 04-foundation-architecture
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - Kindred/KindredApp/App/KindredApp.swift
  - Kindred/KindredApp/App/AppReducer.swift
  - Kindred/KindredApp/App/RootView.swift
  - Kindred/KindredApp/Launch/SplashView.swift
  - Kindred/KindredApp/Launch/WelcomeCardView.swift
  - Kindred/Packages/FeedFeature/Sources/FeedView.swift
  - Kindred/Packages/FeedFeature/Sources/FeedReducer.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileReducer.swift
  - Kindred/Packages/DesignSystem/Sources/Utilities/HapticFeedback.swift
autonomous: false

requirements: []

must_haves:
  truths:
    - "App shows animated splash screen on launch (logo fade-in with pulse)"
    - "First launch shows single dismissible welcome card before feed"
    - "Returning users skip welcome card and go directly to feed"
    - "Tab bar displays Feed (house.fill) and Me (person.fill) tabs with Kindred accent tint"
    - "Tab bar is always visible and never hides on scroll"
    - "Feed tab shows themed placeholder with skeleton shimmer loading state"
    - "Me tab shows guest sign-in gate placeholder"
    - "All UI uses DesignSystem colors and typography (no hardcoded values)"
    - "Dark mode renders warm dark palette throughout"
  artifacts:
    - path: "Kindred/KindredApp/Launch/SplashView.swift"
      provides: "Animated logo splash screen"
    - path: "Kindred/KindredApp/Launch/WelcomeCardView.swift"
      provides: "First-launch welcome card overlay"
    - path: "Kindred/Packages/DesignSystem/Sources/Utilities/HapticFeedback.swift"
      provides: "Haptic feedback utility respecting accessibility settings"
      exports: ["HapticFeedback"]
    - path: "Kindred/KindredApp/App/RootView.swift"
      provides: "Themed TabView with DesignSystem colors"
    - path: "Kindred/Packages/FeedFeature/Sources/FeedView.swift"
      provides: "Feed tab with skeleton loading and DesignSystem styling"
    - path: "Kindred/Packages/ProfileFeature/Sources/ProfileView.swift"
      provides: "Me tab with guest sign-in gate"
  key_links:
    - from: "Kindred/KindredApp/App/KindredApp.swift"
      to: "SplashView → RootView"
      via: "showSplash state controls transition"
      pattern: "showSplash.*SplashView.*RootView"
    - from: "Kindred/KindredApp/App/RootView.swift"
      to: "DesignSystem Colors"
      via: ".tint(.kindredAccent) and .background(.kindredBackground)"
      pattern: "\\.kindredAccent|\\.kindredBackground"
    - from: "Kindred/Packages/FeedFeature/Sources/FeedView.swift"
      to: "DesignSystem Components"
      via: "Uses SkeletonShimmer, CardSurface, Typography"
      pattern: "SkeletonShimmer|CardSurface|kindredBody"
    - from: "Kindred/Packages/DesignSystem/Sources/Utilities/HapticFeedback.swift"
      to: "UIAccessibility"
      via: "Checks isReduceMotionEnabled before triggering"
      pattern: "isReduceMotionEnabled"
---

<objective>
Wire the complete app shell: splash screen animation, first-launch welcome card, themed tab navigation with DesignSystem integration, feed/profile placeholder screens, and haptic feedback utility. This makes the app feel like a real product when launched.

Purpose: This is the final integration plan — connecting DesignSystem colors/components (Plan 02) with the app structure (Plan 01) and networking foundation (Plan 03). The result is an app that launches beautifully, shows themed tabs, and is ready for feature work in Phase 5+.

Output: Complete app launch flow (splash → welcome card → feed), themed navigation, styled placeholder screens, haptic utility.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-foundation-architecture/04-CONTEXT.md
@.planning/phases/04-foundation-architecture/04-RESEARCH.md
@.planning/phases/04-foundation-architecture/04-01-SUMMARY.md
@.planning/phases/04-foundation-architecture/04-02-SUMMARY.md
@.planning/phases/04-foundation-architecture/04-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 (DesignSystem) -->
Color extensions: .kindredPrimary, .kindredAccent, .kindredAccentDecorative, .kindredBackground, .kindredCardSurface, .kindredTextPrimary, .kindredTextSecondary
Font extensions: .kindredLargeTitle(), .kindredHeading1(), .kindredHeading2(), .kindredBody(), .kindredCaption()
Spacing: KindredSpacing.xs/.sm/.md/.lg/.xl/.xxl
Components: KindredButton(title, style, action), CardSurface { content }, .shimmer() modifier, ErrorStateView, EmptyStateView

<!-- From Plan 03 (NetworkClient + AuthClient) -->
ApolloClientFactory.create(tokenProvider:) -> ApolloClient
ClerkAuthClient: getToken() async -> String?, isAuthenticated, currentUser
AuthState: .guest | .authenticated(ClerkUser) | .loading

<!-- From Plan 01 (TCA structure) -->
AppReducer: State (feedState, profileState, selectedTab), Action (feed, profile, tabSelected)
FeedReducer: State (isLoading), Action (onAppear)
ProfileReducer: State, Action
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build splash screen, welcome card, and app launch flow</name>
  <files>
    Kindred/KindredApp/Launch/SplashView.swift,
    Kindred/KindredApp/Launch/WelcomeCardView.swift,
    Kindred/KindredApp/App/KindredApp.swift,
    Kindred/KindredApp/App/AppReducer.swift
  </files>
  <action>
    **SplashView.swift** — Animated logo splash screen (per locked decision):
    - Background: `.kindredBackground` (adapts to light/dark mode)
    - App icon/logo image centered (use placeholder Image(systemName: "fork.knife.circle.fill") if no app icon asset exists yet — replace with actual asset when available)
    - Animation: fade in (0→1 opacity) + subtle scale pulse (1.0→1.05→1.0) over 0.8s with easeInOut timing
    - After 1.5s total display time, transition out with opacity fade
    - `@Binding var showSplash: Bool` — parent controls dismissal
    - Uses DesignSystem colors only

    **WelcomeCardView.swift** — First-launch dismissible card (per locked decision):
    - Overlay card on top of feed content (not a separate screen)
    - Card content: "Kindred discovers viral recipes near you. Swipe to explore." (exact copy from locked decision)
    - Warm cream/terracotta card using `CardSurface` component
    - Dismiss button: "Let's Go" using `KindredButton(.primary)`
    - Single tap dismisses card permanently — save to `@AppStorage("hasSeenWelcome")` UserDefaults flag
    - Card appears centered with slight scale-in animation
    - Uses `.kindredHeading2()` for headline, `.kindredBody()` for message

    **KindredApp.swift** — Wire the launch flow:
    ```swift
    @main
    struct KindredApp: App {
        @UIApplicationDelegateAdaptor(AppDelegate.self) var delegate
        @State private var showSplash = true
        @AppStorage("hasSeenWelcome") private var hasSeenWelcome = false

        var body: some Scene {
            WindowGroup {
                ZStack {
                    if showSplash {
                        SplashView(showSplash: $showSplash)
                    } else {
                        RootView(store: store, showWelcome: !hasSeenWelcome) {
                            hasSeenWelcome = true
                        }
                    }
                }
            }
        }
    }
    ```

    The Store should be created at the KindredApp level and passed to RootView. Initialize ApolloClient and ClerkAuthClient in AppDelegate or as lazy properties, inject into TCA dependencies.

    **AppReducer updates:**
    - Add `showWelcome: Bool` to State (controlled by parent, not reducer)
    - OR keep welcome card logic in SwiftUI view layer with @AppStorage (simpler, welcome card has no business logic)
    - Prefer the simpler approach: welcome card is pure UI, no TCA involvement needed

    **CRITICAL per locked decisions:**
    - First launch: splash → welcome card → feed (this exact order)
    - Returning users: splash → feed directly (skip welcome card)
    - Tab bar always visible — welcome card overlays content, does NOT replace it
  </action>
  <verify>
    <automated>cd Kindred && xcodebuild -project Kindred.xcodeproj -scheme Kindred -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -5</automated>
  </verify>
  <done>SplashView shows animated logo with fade-in and pulse animation on app launch. WelcomeCardView displays "Kindred discovers viral recipes near you. Swipe to explore." on first launch only. Second launch skips welcome card. Both views use DesignSystem colors and typography. Transition from splash to main content is smooth.</done>
</task>

<task type="auto">
  <name>Task 2: Theme tab navigation and create styled feed/profile placeholders with haptic utility</name>
  <files>
    Kindred/KindredApp/App/RootView.swift,
    Kindred/Packages/FeedFeature/Sources/FeedView.swift,
    Kindred/Packages/FeedFeature/Sources/FeedReducer.swift,
    Kindred/Packages/ProfileFeature/Sources/ProfileView.swift,
    Kindred/Packages/ProfileFeature/Sources/ProfileReducer.swift,
    Kindred/Packages/DesignSystem/Sources/Utilities/HapticFeedback.swift
  </files>
  <action>
    **RootView.swift** — Apply DesignSystem theming to TabView:
    - `.tint(.kindredAccent)` for tab bar active color
    - Tab bar background: use `.toolbarBackground(.kindredCardSurface, for: .tabBar)` and `.toolbarBackground(.visible, for: .tabBar)` for explicit background
    - Feed tab: `Label("Feed", systemImage: "house.fill")` with badge count support (placeholder, badge logic in Phase 5)
    - Me tab: `Label("Me", systemImage: "person.fill")`
    - Tab bar always visible — do NOT use `.tabViewStyle(.page)` or any style that hides the tab bar
    - Background: `.kindredBackground`
    - Wire scoped stores from AppReducer to FeedView and ProfileView

    **FeedView.swift** — Styled feed placeholder:
    - NavigationStack with navigation title showing location badge ("Istanbul" placeholder)
    - Background: `.kindredBackground`
    - Content: 3 skeleton card placeholders using `CardSurface` + `.redacted(reason: .placeholder)` + `.shimmer()` modifier
    - Each skeleton card mimics recipe card layout: image area (16:9 ratio), title line, subtitle line, metadata row
    - When not loading: show `EmptyStateView` with "Tap to explore viral recipes near you" message
    - Uses all DesignSystem typography and colors — zero hardcoded values
    - `.onAppear { store.send(.onAppear) }`

    **FeedReducer.swift** — Expand with placeholder structure for Phase 5:
    - State: `isLoading`, `location: String = "Istanbul"` (default per locked decision: Istanbul when location denied)
    - Action: `onAppear`, `locationChanged(String)`
    - No actual network calls yet — just state management skeleton

    **ProfileView.swift** — Guest sign-in gate:
    - Background: `.kindredBackground`
    - Content: Guest state card showing "Sign in to access your profile, voice settings, and saved recipes"
    - Large SF Symbol (person.crop.circle) in `.kindredAccentDecorative`
    - "Sign In" button using `KindredButton(.primary)` — no action yet (Phase 8 handles auth flow)
    - "Continue as Guest" text link below
    - Per locked decision: "Guest users see full sign-in gate on Me tab (no settings access until authenticated)"

    **ProfileReducer.swift** — Expand with auth state:
    - State: `authState: AuthState = .guest`
    - Action: `signInTapped`, `continueAsGuestTapped`
    - No actual auth logic — placeholder actions for Phase 8

    **HapticFeedback.swift** — Utility in DesignSystem/Utilities/:
    ```swift
    import UIKit

    public enum HapticFeedback {
        /// Light impact — tab selection, minor actions
        public static func light() {
            guard !UIAccessibility.isReduceMotionEnabled else { return }
            UIImpactFeedbackGenerator(style: .light).impactOccurred()
        }

        /// Medium impact — bookmark, voice play start
        public static func medium() {
            guard !UIAccessibility.isReduceMotionEnabled else { return }
            UIImpactFeedbackGenerator(style: .medium).impactOccurred()
        }

        /// Success notification — save complete, action confirmed
        public static func success() {
            guard !UIAccessibility.isReduceMotionEnabled else { return }
            UINotificationFeedbackGenerator().notificationOccurred(.success)
        }

        /// Selection — subtle feedback for picker/toggle changes
        public static func selection() {
            guard !UIAccessibility.isReduceMotionEnabled else { return }
            UISelectionFeedbackGenerator().selectionChanged()
        }
    }
    ```

    Per locked decisions:
    - Haptics for key moments: swipe bookmark, voice play start, successful save
    - Respect iOS system haptic setting (checks `isReduceMotionEnabled`)
    - No separate in-app toggle needed

    **AVOID:**
    - Do NOT hardcode any colors or font sizes — everything from DesignSystem
    - Do NOT hide tab bar on scroll or during any interaction
    - Do NOT add haptics to scroll or continuous gestures (Pitfall 7: battery drain)
    - Do NOT implement actual network calls — placeholder state only (Phase 5+ handles data)
  </action>
  <verify>
    <automated>cd Kindred && xcodebuild -project Kindred.xcodeproj -scheme Kindred -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -5</automated>
  </verify>
  <done>TabView themed with Kindred accent color and warm background. Feed tab shows skeleton loading placeholders with shimmer animation. Me tab shows guest sign-in gate with "Sign In" button. Tab bar always visible with house.fill and person.fill icons. HapticFeedback utility available in DesignSystem package. All views use DesignSystem colors and typography exclusively. Dark mode renders warm dark palette.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete app foundation</name>
  <files>Kindred/KindredApp/App/KindredApp.swift</files>
  <action>
    Human verifies the complete app foundation visually in Xcode simulator.

    What was built: Complete Kindred iOS app foundation — animated splash screen, welcome card on first launch, themed 2-tab navigation (Feed with skeleton loading, Me with guest sign-in gate), light + dark mode with warm color palette, DesignSystem package with reusable components, Apollo GraphQL client with Clerk auth, haptic feedback utility.

    How to verify:
    1. Open Kindred.xcodeproj in Xcode
    2. Build and run on iPhone 16 simulator (iOS 17+)
    3. Splash screen: App shows animated logo with fade-in pulse, then transitions to main screen
    4. Welcome card (first launch): Dismissible card with "Kindred discovers viral recipes near you. Swipe to explore." appears. Tap "Let's Go" to dismiss.
    5. Tab navigation: Bottom tab bar shows Feed (house icon) and Me (person icon). Tapping switches between tabs. Tab bar is always visible.
    6. Feed tab: Shows skeleton loading cards with shimmer animation
    7. Me tab: Shows guest sign-in gate with "Sign In" button
    8. Dark mode test: Go to Simulator Features Toggle Appearance. Verify background becomes warm dark brown (not cold gray/blue), card surfaces become deep brown, text is readable, tab bar adapts.
    9. Re-launch: Stop and re-launch the app. Welcome card should NOT appear again.
    10. Overall feel: App should feel warm, inviting, like a cozy kitchen app — not a cold tech app
  </action>
  <verify>
    <automated>cd Kindred && xcodebuild -project Kindred.xcodeproj -scheme Kindred -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | grep -E "(BUILD SUCCEEDED|error:)" | head -5</automated>
  </verify>
  <done>Human approves visual appearance of splash screen, welcome card, tab navigation, feed skeleton, profile gate, and both light and dark mode themes. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
1. `xcodebuild build` succeeds with all packages and app shell
2. Full launch flow works: splash → welcome (first only) → feed
3. Tab navigation functional with themed colors
4. Dark mode renders warm palette throughout
5. Feed shows skeleton shimmer, Me shows guest gate
6. Welcome card persists dismissal across launches
7. All UI uses DesignSystem tokens (no hardcoded values)
</verification>

<success_criteria>
- Splash animation plays on every launch (fade-in + subtle pulse)
- Welcome card appears on first launch only, dismissed permanently after tap
- TabView shows 2 tabs: Feed (house.fill) and Me (person.fill) with Kindred accent tint
- Tab bar always visible (never hides)
- Feed tab: skeleton shimmer loading placeholders
- Me tab: guest sign-in gate with themed button
- Light mode: cream/terracotta palette
- Dark mode: warm dark browns/deep terracotta (NOT cold)
- HapticFeedback utility respects accessibility reduce motion setting
- Human approves visual appearance in both light and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation-architecture/04-04-SUMMARY.md`
</output>
