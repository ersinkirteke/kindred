---
phase: 04-foundation-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Kindred/Kindred.xcodeproj/project.pbxproj
  - Kindred/KindredApp/App/KindredApp.swift
  - Kindred/KindredApp/App/AppDelegate.swift
  - Kindred/Packages/DesignSystem/Package.swift
  - Kindred/Packages/DesignSystem/Sources/DesignSystem.swift
  - Kindred/Packages/NetworkClient/Package.swift
  - Kindred/Packages/NetworkClient/Sources/NetworkClient.swift
  - Kindred/Packages/AuthClient/Package.swift
  - Kindred/Packages/AuthClient/Sources/AuthClient.swift
  - Kindred/Packages/FeedFeature/Package.swift
  - Kindred/Packages/FeedFeature/Sources/FeedReducer.swift
  - Kindred/Packages/FeedFeature/Sources/FeedView.swift
  - Kindred/Packages/ProfileFeature/Package.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileReducer.swift
  - Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
  - Kindred/KindredApp/App/AppReducer.swift
  - Kindred/KindredApp/App/RootView.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "Xcode project opens and builds without errors on iOS 17.0+ target"
    - "SPM local packages resolve and compile (DesignSystem, NetworkClient, AuthClient, FeedFeature, ProfileFeature)"
    - "App launches in simulator showing a TabView with Feed and Me tabs"
    - "TCA AppReducer scopes state correctly to FeedReducer and ProfileReducer"
  artifacts:
    - path: "Kindred/Kindred.xcodeproj/project.pbxproj"
      provides: "Xcode project configuration"
    - path: "Kindred/Packages/DesignSystem/Package.swift"
      provides: "DesignSystem SPM package manifest"
    - path: "Kindred/Packages/NetworkClient/Package.swift"
      provides: "NetworkClient SPM package manifest"
    - path: "Kindred/Packages/AuthClient/Package.swift"
      provides: "AuthClient SPM package manifest"
    - path: "Kindred/Packages/FeedFeature/Package.swift"
      provides: "FeedFeature SPM package with TCA dependency"
    - path: "Kindred/Packages/ProfileFeature/Package.swift"
      provides: "ProfileFeature SPM package with TCA dependency"
    - path: "Kindred/KindredApp/App/AppReducer.swift"
      provides: "Root TCA reducer composing FeedReducer and ProfileReducer"
    - path: "Kindred/KindredApp/App/RootView.swift"
      provides: "TabView container with Feed and Me tabs"
  key_links:
    - from: "Kindred/KindredApp/App/KindredApp.swift"
      to: "AppReducer + RootView"
      via: "SwiftUI App entry point creates Store<AppReducer>"
      pattern: "Store.*AppReducer"
    - from: "Kindred/KindredApp/App/AppReducer.swift"
      to: "FeedReducer, ProfileReducer"
      via: "TCA Scope composition"
      pattern: "Scope.*feedState.*FeedReducer"
    - from: "Kindred/KindredApp/App/RootView.swift"
      to: "FeedView, ProfileView"
      via: "TabView scoped stores"
      pattern: "TabView.*store\\.scope"
---

<objective>
Create the Kindred iOS Xcode project with modular Swift Package Manager architecture and TCA (Composable Architecture) root state management. This establishes the foundational project structure that all subsequent phases build upon.

Purpose: Every Phase 5-10 feature needs a compilable project, modular packages for isolated development, and a TCA store hierarchy for state management. Without this, nothing else can be built.

Output: Xcode project with 5 local SPM packages, TCA AppReducer composing child features, and a working TabView shell.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-foundation-architecture/04-CONTEXT.md
@.planning/phases/04-foundation-architecture/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with SPM local packages</name>
  <files>
    Kindred/Kindred.xcodeproj/project.pbxproj,
    Kindred/KindredApp/App/KindredApp.swift,
    Kindred/KindredApp/App/AppDelegate.swift,
    Kindred/KindredApp/Resources/Info.plist,
    Kindred/Packages/DesignSystem/Package.swift,
    Kindred/Packages/DesignSystem/Sources/DesignSystem.swift,
    Kindred/Packages/NetworkClient/Package.swift,
    Kindred/Packages/NetworkClient/Sources/NetworkClient.swift,
    Kindred/Packages/AuthClient/Package.swift,
    Kindred/Packages/AuthClient/Sources/AuthClient.swift,
    Kindred/Packages/FeedFeature/Package.swift,
    Kindred/Packages/FeedFeature/Sources/FeedPlaceholder.swift,
    Kindred/Packages/ProfileFeature/Package.swift,
    Kindred/Packages/ProfileFeature/Sources/ProfilePlaceholder.swift
  </files>
  <action>
    Create the Xcode project from scratch using `xcodebuild` or by writing the project files directly.

    **Project configuration:**
    - Project name: `Kindred`
    - Bundle ID: `com.kindred.app`
    - Deployment target: iOS 17.0 (required by Clerk SDK)
    - Swift version: 5.10+
    - Development team: leave blank (user configures)

    **SPM Package structure** — create 5 local packages under `Kindred/Packages/`:

    1. **DesignSystem** (no external dependencies yet)
       - `Package.swift` with platforms: [.iOS(.v17)]
       - `Sources/DesignSystem.swift` — placeholder public enum DesignSystem {}

    2. **NetworkClient** (depends on Apollo iOS 2.0.6+, ApolloSQLite)
       - `Package.swift` with apollo-ios dependency
       - `Sources/NetworkClient.swift` — placeholder public class NetworkClient {}

    3. **AuthClient** (depends on Clerk iOS SDK 1.0.0+)
       - `Package.swift` with clerk-ios dependency
       - `Sources/AuthClient.swift` — placeholder public class AuthClient {}

    4. **FeedFeature** (depends on DesignSystem, swift-composable-architecture)
       - `Package.swift` with TCA + DesignSystem local dependency
       - `Sources/FeedPlaceholder.swift` — minimal public struct

    5. **ProfileFeature** (depends on DesignSystem, swift-composable-architecture)
       - `Package.swift` with TCA + DesignSystem local dependency
       - `Sources/ProfilePlaceholder.swift` — minimal public struct

    **App target:**
    - `KindredApp.swift` — SwiftUI @main entry point (basic WindowGroup with Text("Kindred") placeholder)
    - `AppDelegate.swift` — UIApplicationDelegateAdaptor for Firebase and Kingfisher cache config (stubs only, configured in Plan 03)
    - `Info.plist` — standard iOS app info with location usage description strings (NSLocationWhenInUseUsageDescription)

    **SPM dependency URLs:**
    ```
    swift-composable-architecture: https://github.com/pointfreeco/swift-composable-architecture (from: "1.0.0")
    apollo-ios: https://github.com/apollographql/apollo-ios (from: "2.0.6")
    clerk-ios: https://github.com/clerk/clerk-ios (from: "1.0.0")
    Kingfisher: https://github.com/onevcat/Kingfisher (from: "8.0.0")
    firebase-ios-sdk: https://github.com/firebase/firebase-ios-sdk (from: "11.0.0")
    ```

    **IMPORTANT:** Use `swift package init` for each local package, then edit Package.swift to add dependencies. The main Xcode project references local packages via relative paths. Each package must have at least one public symbol to compile.

    **AVOID:** Do NOT use CocoaPods. Do NOT set deployment target below iOS 17.0.
  </action>
  <verify>
    <automated>cd Kindred && xcodebuild -project Kindred.xcodeproj -scheme Kindred -destination 'platform=iOS Simulator,name=iPhone 16' -resolvePackageDependencies 2>&1 | tail -5</automated>
  </verify>
  <done>Xcode project opens, all 5 SPM packages resolve dependencies, project builds (even if with placeholder content). iOS 17.0 deployment target set.</done>
</task>

<task type="auto">
  <name>Task 2: Set up TCA AppReducer and TabView root navigation</name>
  <files>
    Kindred/KindredApp/App/AppReducer.swift,
    Kindred/KindredApp/App/RootView.swift,
    Kindred/KindredApp/App/KindredApp.swift,
    Kindred/Packages/FeedFeature/Sources/FeedReducer.swift,
    Kindred/Packages/FeedFeature/Sources/FeedView.swift,
    Kindred/Packages/ProfileFeature/Sources/ProfileReducer.swift,
    Kindred/Packages/ProfileFeature/Sources/ProfileView.swift
  </files>
  <action>
    Wire TCA state management from root to child features, and create the 2-tab navigation.

    **FeedReducer** (in FeedFeature package):
    ```swift
    @Reducer
    struct FeedReducer {
        @ObservableState
        struct State: Equatable {
            // Placeholder — populated in Phase 5
            var isLoading = false
        }

        enum Action {
            case onAppear
        }

        var body: some ReducerOf<Self> {
            Reduce { state, action in
                switch action {
                case .onAppear:
                    return .none
                }
            }
        }
    }
    ```

    **FeedView** — simple placeholder view showing "Feed" text, styled with DesignSystem once available.

    **ProfileReducer** (in ProfileFeature package):
    - Similar minimal structure with State and Action
    - Guest state placeholder (no auth logic yet — comes in Plan 03 auth setup)

    **ProfileView** — simple placeholder showing "Me" text.

    **AppReducer** (in main app target):
    ```swift
    @Reducer
    struct AppReducer {
        @ObservableState
        struct State: Equatable {
            var feedState = FeedReducer.State()
            var profileState = ProfileReducer.State()
            var selectedTab: Tab = .feed
        }

        enum Tab: Int, Equatable {
            case feed = 0
            case me = 1
        }

        enum Action {
            case feed(FeedReducer.Action)
            case profile(ProfileReducer.Action)
            case tabSelected(Tab)
        }

        var body: some ReducerOf<Self> {
            Scope(state: \.feedState, action: \.feed) { FeedReducer() }
            Scope(state: \.profileState, action: \.profile) { ProfileReducer() }
            Reduce { state, action in
                switch action {
                case .tabSelected(let tab):
                    state.selectedTab = tab
                    return .none
                case .feed, .profile:
                    return .none
                }
            }
        }
    }
    ```

    **RootView** — TabView with 2 tabs:
    - Feed tab: SF Symbol "house.fill", label "Feed" (per Claude's discretion in CONTEXT.md)
    - Me tab: SF Symbol "person.fill", label "Me"
    - Tab bar always visible (per locked decision — no `.tabViewStyle` that hides it)
    - Tint color: `.kindredAccent` (placeholder Color.orange until DesignSystem wired in Plan 02)
    - Use `store.scope` to pass scoped stores to child views

    **KindredApp.swift** — Update to create `Store(initialState: AppReducer.State()) { AppReducer() }` and render RootView.

    **Tab state persistence:** Hoist selectedTab in AppReducer.State so tab selection survives child view recreations (avoids Pitfall 5 from research: SwiftUI TabView state loss).

    **AVOID:**
    - Do NOT use deprecated WithViewStore — use @ObservableState macro (TCA 1.7+ per research)
    - Do NOT observe entire State in views — scope to minimal needed fields
  </action>
  <verify>
    <automated>cd Kindred && xcodebuild -project Kindred.xcodeproj -scheme Kindred -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -10</automated>
  </verify>
  <done>App builds and launches in simulator. TabView displays 2 tabs (Feed with house.fill icon, Me with person.fill icon). Tapping tabs switches between FeedView and ProfileView placeholders. TCA AppReducer correctly scopes state to child reducers.</done>
</task>

</tasks>

<verification>
1. `xcodebuild build` succeeds with zero errors on iOS 17.0 target
2. App launches in iPhone 16 simulator
3. Two tabs visible at bottom: "Feed" (house.fill) and "Me" (person.fill)
4. Tab switching works — each tab shows its placeholder view
5. SPM packages (DesignSystem, NetworkClient, AuthClient, FeedFeature, ProfileFeature) all compile
</verification>

<success_criteria>
- Xcode project opens and builds without errors
- 5 local SPM packages defined and compilable
- TCA AppReducer composes FeedReducer and ProfileReducer
- TabView with Feed and Me tabs renders in simulator
- iOS 17.0 deployment target confirmed
- All external SPM dependencies resolve (TCA, Apollo, Clerk, Kingfisher, Firebase)
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation-architecture/04-01-SUMMARY.md`
</output>
