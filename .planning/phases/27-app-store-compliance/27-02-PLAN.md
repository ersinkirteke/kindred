---
phase: 27-app-store-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift
  - Kindred/Sources/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - STORE-03
must_haves:
  truths:
    - "RecipeDetailView shows a nutrition disclaimer footer at the bottom of the scrollable content (after StepTimelineView, before bottomBar)"
    - "Footer contains a localized disclaimer string stating nutrition estimates come from Spoonacular and are not for medical use"
    - "Footer contains a tappable 'Powered by Spoonacular' link that opens https://spoonacular.com/food-api in the browser"
    - "Footer uses kindredTextSecondary (NOT kindredTextTertiary — that color does not exist in DesignSystem) and kindredCaptionScaled for Dynamic Type support"
    - "Localizable.xcstrings contains en and tr translations for both the disclaimer and the attribution link"
    - "FeedFeature SPM package still builds without warnings after the edit"
  artifacts:
    - path: "Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift"
      provides: "Recipe detail view with compliance footer"
      contains: "nutrition_disclaimer"
    - path: "Kindred/Sources/Resources/Localizable.xcstrings"
      provides: "Localized compliance strings (en + tr)"
      contains: "nutrition_disclaimer"
  key_links:
    - from: "RecipeDetailView.recipeContentView"
      to: "Localizable.xcstrings keys nutrition_disclaimer + powered_by_spoonacular"
      via: "String(localized:, bundle: .main)"
      pattern: "String\\(localized: \"nutrition_disclaimer\""
    - from: "RecipeDetailView compliance footer Link"
      to: "https://spoonacular.com/food-api"
      via: "SwiftUI Link(destination:)"
      pattern: "spoonacular\\.com/food-api"
---

<objective>
Add a compliance footer to `RecipeDetailView.swift` containing a nutrition-use disclaimer and a "Powered by Spoonacular" attribution link, and add the two new user-facing strings to `Localizable.xcstrings` with both English and Turkish translations.

Purpose: STORE-03 success criterion #6 requires Spoonacular attribution visible on the recipe detail view, and the broader Phase 27 compliance goal requires a nutrition disclaimer for Spoonacular health data (Guideline 1.4.1). A single inline footer satisfies both needs without cluttering the recipe content.

Output:
- `RecipeDetailView.swift` with a new `complianceFooter` view appended to `recipeContentView(_:)` after `StepTimelineView` at line 190
- `Localizable.xcstrings` with two new entries: `nutrition_disclaimer` and `powered_by_spoonacular` (both with en + tr)
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-app-store-compliance/27-CONTEXT.md
@.planning/phases/27-app-store-compliance/27-RESEARCH.md
@Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift

<interfaces>
<!-- Key DesignSystem tokens and patterns the executor needs. Verified in codebase. -->

From Kindred/Packages/DesignSystem/Sources/DesignSystem/Colors.swift:
- `.kindredTextPrimary` (exists)
- `.kindredTextSecondary` (exists — line 42)
- `.kindredTextTertiary` **DOES NOT EXIST** — DO NOT USE. Use `.kindredTextSecondary`.

From Kindred/Packages/DesignSystem/Sources/DesignSystem/Spacing.swift:
- `KindredSpacing.xs`, `KindredSpacing.sm`, `KindredSpacing.md`, `KindredSpacing.lg`, `KindredSpacing.xl`, `KindredSpacing.xxl`

From Kindred/Packages/DesignSystem/Sources/DesignSystem/Typography.swift:
- `.kindredCaptionScaled(size: captionSize)` — for Dynamic Type-aware caption text

From Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift:
- `@ScaledMetric(relativeTo: .caption) private var captionSize: CGFloat = 14` — already declared at line 17, reuse it
- `recipeContentView(_ recipe: RecipeDetail) -> some View` at line 117 — the outer VStack ends at line 191. Append the footer inside this VStack, immediately after `StepTimelineView(steps: recipe.steps)` at line 190.
- Existing localization pattern: `String(localized: "Loading recipe...", bundle: .main)` — NOTE: Kindred uses the full English string as the key (not a semantic key). Follow this convention for consistency. Keys for this plan's new strings are therefore the full English sentences:
  - Key 1: `"Nutrition estimates from Spoonacular. Not for medical use."`
  - Key 2: `"Powered by Spoonacular"`
  - Key 3: `"Opens Spoonacular website in browser"` (accessibility label only)

From Kindred/Sources/Resources/Localizable.xcstrings (5483 lines, version 1.1):
- Top-level shape: `{ "sourceLanguage": "en", "strings": { "<key>": {...} }, "version": "1.1" }`
- Each string entry follows this shape:
```json
"<key>": {
  "extractionState": "manual",
  "localizations": {
    "en": { "stringUnit": { "state": "translated", "value": "<en text>" } },
    "tr": { "stringUnit": { "state": "translated", "value": "<tr text>" } }
  }
}
```
- New entries should be inserted inside the top-level `"strings"` object. Alphabetical insertion is not required; Xcode String Catalog resorts on next open. Safe insertion point is near the other "Loading..." entry around line 5465 OR at the beginning of the strings object — either works.

**SwiftUI link pattern choice:** Research confirmed no `openURL` environment usage in FeedFeature. Use SwiftUI `Link(destination:)` — simpler and matches Apple's modern recommendation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nutrition_disclaimer + powered_by_spoonacular strings to Localizable.xcstrings</name>
  <files>Kindred/Sources/Resources/Localizable.xcstrings</files>
  <action>
Add THREE new string entries to `Kindred/Sources/Resources/Localizable.xcstrings` inside the top-level `"strings"` object. Kindred's existing convention uses the full English sentence as the key (e.g., `"Loading recipe..."` — see line 93 of RecipeDetailView.swift). Follow this convention.

Insert anywhere inside the `"strings": { ... }` object — a safe location is immediately after the existing `"Loading..."` entry (around line 5481). Maintain valid JSON (trailing comma on previous entry if inserting in the middle, no trailing comma on the last entry).

**Entry 1 — Nutrition disclaimer:**
```json
"Nutrition estimates from Spoonacular. Not for medical use.": {
  "extractionState": "manual",
  "localizations": {
    "en": {
      "stringUnit": {
        "state": "translated",
        "value": "Nutrition estimates from Spoonacular. Not for medical use."
      }
    },
    "tr": {
      "stringUnit": {
        "state": "translated",
        "value": "Besin değerleri Spoonacular tarafından sağlanır. Tıbbi tavsiye için kullanılmaz."
      }
    }
  }
}
```

**Entry 2 — Attribution link text:**
```json
"Powered by Spoonacular": {
  "extractionState": "manual",
  "localizations": {
    "en": {
      "stringUnit": {
        "state": "translated",
        "value": "Powered by Spoonacular"
      }
    },
    "tr": {
      "stringUnit": {
        "state": "translated",
        "value": "Spoonacular tarafından desteklenmektedir"
      }
    }
  }
}
```

**Entry 3 — Accessibility label for the link:**
```json
"Opens Spoonacular website in browser": {
  "extractionState": "manual",
  "localizations": {
    "en": {
      "stringUnit": {
        "state": "translated",
        "value": "Opens Spoonacular website in browser"
      }
    },
    "tr": {
      "stringUnit": {
        "state": "translated",
        "value": "Spoonacular web sitesini tarayıcıda açar"
      }
    }
  }
}
```

**DO NOT:**
- Break JSON validity. Check comma placement before and after the inserted entries.
- Change the top-level `"version": "1.1"` or `"sourceLanguage": "en"` fields.
- Alter any existing string entries.
- Use semantic keys (e.g., `"nutrition_disclaimer"`) — Kindred's convention is full English sentences as keys.
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('Kindred/Sources/Resources/Localizable.xcstrings'))" && grep -q "Nutrition estimates from Spoonacular" Kindred/Sources/Resources/Localizable.xcstrings && grep -q "Powered by Spoonacular" Kindred/Sources/Resources/Localizable.xcstrings && grep -q "Besin değerleri Spoonacular" Kindred/Sources/Resources/Localizable.xcstrings && grep -q "Spoonacular tarafından desteklenmektedir" Kindred/Sources/Resources/Localizable.xcstrings</automated>
  </verify>
  <done>
- `python3 -c "import json; json.load(open('Kindred/Sources/Resources/Localizable.xcstrings'))"` succeeds (valid JSON)
- File contains all three English values verbatim
- File contains both Turkish translations verbatim
- `"version": "1.1"` line at end of file is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Add compliance footer to RecipeDetailView.swift</name>
  <files>Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift</files>
  <action>
Edit `Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift` to append a compliance footer to `recipeContentView(_:)`.

**Insertion point:** Inside the `recipeContentView(_:)` method's outer VStack. Find line 190:
```swift
            StepTimelineView(steps: recipe.steps)
        }
    }
```
The closing `}` at line 191 closes the VStack. Insert the footer BEFORE that closing brace, AFTER the `StepTimelineView` call. Preserve 12-space / 4-tab indentation to match surrounding code.

**Footer code to insert (place immediately after `StepTimelineView(steps: recipe.steps)` on line 190, before the VStack's closing `}`):**
```swift

            // Compliance footer: nutrition disclaimer + Spoonacular attribution (Phase 27 STORE-03)
            // Uses kindredTextSecondary because kindredTextTertiary does not exist in DesignSystem.
            VStack(alignment: .leading, spacing: KindredSpacing.xs) {
                Text(String(localized: "Nutrition estimates from Spoonacular. Not for medical use.", bundle: .main))
                    .font(.kindredCaptionScaled(size: captionSize))
                    .foregroundStyle(.kindredTextSecondary)

                Link(destination: URL(string: "https://spoonacular.com/food-api")!) {
                    HStack(spacing: 4) {
                        Text(String(localized: "Powered by Spoonacular", bundle: .main))
                        Image(systemName: "arrow.up.right")
                    }
                    .font(.kindredCaptionScaled(size: captionSize))
                    .foregroundStyle(.kindredTextSecondary)
                }
                .accessibilityLabel(String(localized: "Opens Spoonacular website in browser", bundle: .main))
                .accessibilityAddTraits(.isLink)
            }
            .padding(.top, KindredSpacing.lg)
            .padding(.bottom, KindredSpacing.md)
            .frame(maxWidth: .infinity, alignment: .leading)
```

**Key implementation notes:**
- **Color:** Use `.kindredTextSecondary` — `.kindredTextTertiary` does NOT exist in DesignSystem/Colors.swift (verified in research). Do not attempt to create it.
- **Font:** Reuse the existing `captionSize` `@ScaledMetric` declared at line 17 (`@ScaledMetric(relativeTo: .caption) private var captionSize: CGFloat = 14`). Do not declare a new scaled metric.
- **Link:** Use SwiftUI `Link(destination:)` — not `Environment(\.openURL)`. Research confirmed no `openURL` usage exists in FeedFeature; `Link` is simpler and localization-friendly.
- **URL force-unwrap:** `URL(string: "https://spoonacular.com/food-api")!` is acceptable here because the string is a compile-time constant known to be valid. Matches the project's existing pattern for hard-coded URLs.
- **Icon:** Use SF Symbol `arrow.up.right` (indicates external link) instead of the `→` character for clarity with VoiceOver.
- **Localization:** All three user-facing strings must go through `String(localized:, bundle: .main)` with the exact keys from Task 1.
- **Accessibility:** `.accessibilityLabel` on the Link explicitly describes the destination. `.accessibilityAddTraits(.isLink)` signals to assistive tech that this opens a URL.
- **Layout:** `.frame(maxWidth: .infinity, alignment: .leading)` matches the leading-aligned surrounding content.

**DO NOT:**
- Add new imports (`SwiftUI` is already imported at line 1).
- Reorder or modify any existing code inside `recipeContentView(_:)`.
- Place the footer inside the bottomBar or sticky section — it must be inside the scrollable content.
- Use hardcoded hex colors or inline font sizes.
- Touch `RecipeDetailReducer.swift` — this is a pure view change, no state or action needed.
  </action>
  <verify>
    <automated>cd Kindred/Packages/FeedFeature && swift build 2>&1 | tee /tmp/27-02-build.log && ! grep -q "error:" /tmp/27-02-build.log && grep -q "Nutrition estimates from Spoonacular" Sources/RecipeDetail/RecipeDetailView.swift && grep -q "spoonacular.com/food-api" Sources/RecipeDetail/RecipeDetailView.swift && grep -q "Powered by Spoonacular" Sources/RecipeDetail/RecipeDetailView.swift && ! grep -q "kindredTextTertiary" Sources/RecipeDetail/RecipeDetailView.swift</automated>
  </verify>
  <done>
- `swift build` in the `FeedFeature` package directory succeeds with no errors (warnings about unrelated code are acceptable)
- `RecipeDetailView.swift` contains `String(localized: "Nutrition estimates from Spoonacular. Not for medical use.", bundle: .main)`
- `RecipeDetailView.swift` contains `Link(destination: URL(string: "https://spoonacular.com/food-api")!)`
- `RecipeDetailView.swift` contains `String(localized: "Powered by Spoonacular", bundle: .main)`
- `RecipeDetailView.swift` does NOT contain `kindredTextTertiary` (does not exist in DesignSystem)
- Footer uses `.kindredTextSecondary` and `.kindredCaptionScaled(size: captionSize)`
- No new imports added; no new `@ScaledMetric` declared
  </done>
</task>

</tasks>

<verification>
**Automated:**
- `python3 -c "import json; json.load(open('Kindred/Sources/Resources/Localizable.xcstrings'))"` succeeds
- `cd Kindred/Packages/FeedFeature && swift build` succeeds
- `grep` confirms all English and Turkish strings landed in the `.xcstrings` file
- `grep` confirms footer code landed in `RecipeDetailView.swift`
- `grep -q "kindredTextTertiary"` returns FALSE (no references to the non-existent color)

**Manual (spot-check on simulator, NOT required for plan completion — Plan 27-04 handles visual confirmation):**
- Footer is visible at the bottom of the recipe detail scroll view (after instructions)
- Tapping the Link opens Safari at `https://spoonacular.com/food-api`
- VoiceOver reads "Opens Spoonacular website in browser" when focusing the link
- Turkish locale shows "Besin değerleri Spoonacular tarafından sağlanır..." disclaimer

**STORE-03 coverage after this plan:** The code and strings exist. Screenshots are refreshed in Plan 27-04 (which depends on this plan).
</verification>

<success_criteria>
- `Localizable.xcstrings` parses as valid JSON and contains both disclaimer and attribution strings with en + tr translations
- `RecipeDetailView.swift` compiles inside `FeedFeature` SPM package with no errors
- Footer code uses `kindredTextSecondary` (NOT `kindredTextTertiary`), `kindredCaptionScaled`, `KindredSpacing.{xs,lg,md}`, and SwiftUI `Link`
- All three user-facing strings route through `String(localized:, bundle: .main)`
- No changes to reducer, store, or any file outside the two listed in `files_modified`
</success_criteria>

<output>
After completion, create `.planning/phases/27-app-store-compliance/27-02-SUMMARY.md` with:
1. Exact code diff for `RecipeDetailView.swift`
2. Exact JSON additions for `Localizable.xcstrings`
3. `swift build` output confirming green build
4. Screenshot (if possible) of the footer rendered on simulator — OR note that visual confirmation is deferred to Plan 27-04
5. Note for Plan 27-04: "Detail screenshot must scroll to show this footer in frame"
</output>
