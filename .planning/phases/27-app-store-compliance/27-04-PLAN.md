---
phase: 27-app-store-compliance
plan: 04
type: execute
wave: 2
depends_on:
  - 27-02
files_modified:
  - Kindred/fastlane/screenshots/en-US/02-recipe-feed.png
  - Kindred/fastlane/screenshots/en-US/05-recipe-detail.png
  - Kindred/fastlane/screenshots/tr/02-recipe-feed.png
  - Kindred/fastlane/screenshots/tr/05-recipe-detail.png
autonomous: false
requirements:
  - STORE-03
must_haves:
  truths:
    - "Four screenshot files (02-recipe-feed.png and 05-recipe-detail.png in en-US and tr) are replaced with new captures taken from the current build of Kindred"
    - "Each new screenshot is 1408 x 3040 pixels (iPhone 16 Pro Max 6.9\" physical size) — the same dimensions as the files they replace"
    - "The replaced 02-recipe-feed.png screenshots show the 'Popular Recipes' heading (the current FeedView hardcoded literal, rendered the same in both locales)"
    - "The replaced 05-recipe-detail.png screenshots show the Spoonacular compliance footer from Plan 27-02 clearly visible in frame"
    - "File modification timestamps on the four replaced files are newer than the start of this plan"
  artifacts:
    - path: "Kindred/fastlane/screenshots/en-US/02-recipe-feed.png"
      provides: "English feed screenshot showing Popular Recipes heading"
    - path: "Kindred/fastlane/screenshots/en-US/05-recipe-detail.png"
      provides: "English detail screenshot showing Spoonacular attribution footer"
    - path: "Kindred/fastlane/screenshots/tr/02-recipe-feed.png"
      provides: "Turkish feed screenshot"
    - path: "Kindred/fastlane/screenshots/tr/05-recipe-detail.png"
      provides: "Turkish detail screenshot showing Turkish-localized disclaimer"
  key_links:
    - from: "Kindred/fastlane/screenshots/en-US/05-recipe-detail.png"
      to: "Compliance footer added by Plan 27-02"
      via: "screenshot capture while the footer is visible in the scroll frame"
      pattern: "visual: 'Powered by Spoonacular'"
    - from: "Kindred/fastlane/screenshots/tr/05-recipe-detail.png"
      to: "Localizable.xcstrings Turkish disclaimer"
      via: "device language set to Turkish before capture"
      pattern: "visual: 'Besin değerleri Spoonacular'"
---

<objective>
Re-capture four App Store screenshots (feed + detail, en-US + tr) so that the submitted App Store binary advertises the current "Popular Recipes" feed and the new Spoonacular compliance footer added in Plan 27-02.

Purpose: STORE-03 success criterion #5 requires screenshots to reflect the "Popular Recipes" feed (not the deprecated "Viral near you" UI), and criterion #6 requires the Spoonacular attribution badge to be visible on the recipe detail view. Without a reshoot, the App Store listing would contradict the current app build.

Output: Four replaced PNG files in `Kindred/fastlane/screenshots/{en-US,tr}/` at exactly 1408 x 3040 pixels.

**This plan is NOT autonomous.** Screenshot capture is a human-only task: there is no Snapfile, no UI test target, and no CLI that can take meaningful screenshots of running app UI on behalf of the user (scroll position, language switching, and footer visibility all require human interaction). The executor sets up the environment, prepares the build, drives the human through capture via a blocking checkpoint task, then runs automated dimension + timestamp verification on the returned files.
</objective>

<execution_context>
@/Users/ersinkirteke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ersinkirteke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-app-store-compliance/27-CONTEXT.md
@.planning/phases/27-app-store-compliance/27-RESEARCH.md
@.planning/phases/27-app-store-compliance/27-02-PLAN.md

<interfaces>
<!-- Screenshot spec. All four target files are 1408 x 3040 PNG (verified via sips 2026-04-06). -->

Existing screenshot inventory (files to REPLACE):
- `Kindred/fastlane/screenshots/en-US/02-recipe-feed.png`  — 1408 x 3040
- `Kindred/fastlane/screenshots/en-US/05-recipe-detail.png` — 1408 x 3040
- `Kindred/fastlane/screenshots/tr/02-recipe-feed.png`      — 1408 x 3040
- `Kindred/fastlane/screenshots/tr/05-recipe-detail.png`    — 1408 x 3040

Files that stay UNCHANGED (do NOT retake):
- `01-voice-narration.png`, `03-pantry-scan.png`, `04-dietary-filters.png` in both locales

Device: iPhone 16 Pro Max (6.9" physical, 1320x2868 logical → 1408x3040 physical pixel dimensions when captured via simulator). Apple's current required screenshot size for 6.9" iPhone category.

Capture methods (any of these produces a 1408x3040 PNG):
- `xcrun simctl io booted screenshot <path>` CLI
- Xcode Simulator menu: File → New Screenshot (⌘S) while iPhone 16 Pro Max simulator is focused
- Physical iPhone 16 Pro Max: Side button + Volume Up, then AirDrop/Files to Mac

**Feed heading gotcha (important for TR screenshots):**
`Kindred/Packages/FeedFeature/Sources/Feed/FeedView.swift:142` hardcodes `Text("Popular Recipes")` — it is NOT routed through `String(localized:, bundle:)`. As a result, BOTH en-US and tr builds render the heading as "Popular Recipes" in English. This matches the ROADMAP success criterion text (which says "Popular Recipes"), but the TR screenshot will not show a Turkish translation of the heading. This is a known current-state constraint; do NOT edit the Swift code to localize the heading — feed heading localization is out of scope for Phase 27. Capture what the build actually renders.

**Feed screenshot requirements:**
- Must show the `Popular Recipes` heading in the top portion of the frame
- Must show multiple recipe cards with Spoonacular imagery
- Should NOT show debug overlays, sheets, modals, or banner ads over the main content
- Status bar: typical (e.g., 9:41 AM, full battery, full signal) — use `xcrun simctl status_bar` to stage if needed

**Detail screenshot requirements:**
- MUST show the Spoonacular attribution footer ("Powered by Spoonacular →") clearly visible in frame
- Required scroll position: after the instructions (StepTimelineView), so the footer is near the bottom of the visible viewport but fully on-screen
- The disclaimer text ("Nutrition estimates from Spoonacular. Not for medical use.") should be visible immediately above the link
- For the `tr` variant, the disclaimer must show the Turkish text: "Besin değerleri Spoonacular tarafından sağlanır. Tıbbi tavsiye için kullanılmaz."
- Do NOT capture only the top (hero image + title) — criterion #6 is about the attribution footer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare build and simulator for screenshot capture</name>
  <files>Kindred/fastlane/screenshots/en-US/.staging.txt, Kindred/fastlane/screenshots/tr/.staging.txt</files>
  <action>
Before handing off to the human for capture, prepare the environment so the user can take screenshots in one sitting with minimal friction.

**Step 1 — Verify Plan 27-02 landed:**
```bash
grep -q "Powered by Spoonacular" Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift || { echo "BLOCKER: Plan 27-02 footer not found. Do not proceed."; exit 1; }
grep -q "Nutrition estimates from Spoonacular" Kindred/Sources/Resources/Localizable.xcstrings || { echo "BLOCKER: Plan 27-02 disclaimer not found. Do not proceed."; exit 1; }
```
If either grep fails, the checkpoint cannot proceed — escalate to the human to run Plan 27-02 first.

**Step 2 — Confirm target dimensions of existing files:**
```bash
sips -g pixelWidth -g pixelHeight Kindred/fastlane/screenshots/en-US/02-recipe-feed.png
sips -g pixelWidth -g pixelHeight Kindred/fastlane/screenshots/en-US/05-recipe-detail.png
sips -g pixelWidth -g pixelHeight Kindred/fastlane/screenshots/tr/02-recipe-feed.png
sips -g pixelWidth -g pixelHeight Kindred/fastlane/screenshots/tr/05-recipe-detail.png
```
All four should print `pixelWidth: 1408` and `pixelHeight: 3040`. Record the output in the plan summary. If any differ, note the mismatch in Task 2's message to the human.

**Step 3 — Locate an iPhone 16 Pro Max simulator:**
```bash
xcrun simctl list devices available | grep "iPhone 16 Pro Max"
```
Record the first available device UDID. If none exists, instruct the human (via Task 2) to create one via Xcode → Settings → Platforms.

**Step 4 — Record a baseline timestamp** so the verification task in Task 3 can detect that new files were actually captured (not just re-saved):
```bash
date +%s > /tmp/27-04-baseline-epoch.txt
```

**Step 5 — Create a short staging breadcrumb file** in each screenshot directory to capture the checkpoint hand-off state:

Write `Kindred/fastlane/screenshots/en-US/.staging.txt` with contents:
```
Phase 27 plan 04 staging begun: <ISO8601 timestamp>
Baseline dimensions: 1408 x 3040
Target files: 02-recipe-feed.png, 05-recipe-detail.png
Feature branch must include Plan 27-02 footer for 05-recipe-detail.png to show compliance footer.
```

Write `Kindred/fastlane/screenshots/tr/.staging.txt` with the same contents but locale noted as `tr`.

These `.staging.txt` files are removed by Task 3's cleanup step — they are breadcrumbs, not shipped artifacts.

**DO NOT:**
- Attempt to capture screenshots programmatically in this task — scroll position, language switching, and footer visibility all require human interaction.
- Build the app for real in this task — the human will launch Xcode from the simulator they already use. Setting up a new build here is wasted work.
- Modify the four target PNG files in this task — they stay at their current state until the checkpoint completes.
- Edit FeedView.swift to localize the "Popular Recipes" heading — out of Phase 27 scope.
  </action>
  <verify>
    <automated>test -f Kindred/fastlane/screenshots/en-US/.staging.txt && test -f Kindred/fastlane/screenshots/tr/.staging.txt && test -f /tmp/27-04-baseline-epoch.txt && grep -q "Powered by Spoonacular" Kindred/Packages/FeedFeature/Sources/RecipeDetail/RecipeDetailView.swift</automated>
  </verify>
  <done>
- Plan 27-02 footer and strings are confirmed present in the codebase
- All four existing screenshots verified at 1408 x 3040
- iPhone 16 Pro Max simulator identified (or human instructed to create one)
- Baseline epoch recorded in `/tmp/27-04-baseline-epoch.txt`
- `.staging.txt` breadcrumbs exist in both locale directories
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human captures the four replacement screenshots</name>
  <files>Kindred/fastlane/screenshots/en-US/02-recipe-feed.png, Kindred/fastlane/screenshots/en-US/05-recipe-detail.png, Kindred/fastlane/screenshots/tr/02-recipe-feed.png, Kindred/fastlane/screenshots/tr/05-recipe-detail.png</files>
  <action>
This is a blocking human-verify checkpoint. The executor pauses here and prints the following capture instructions to the user, then waits for the user to reply with "approved" (or an error message).

---

## Screenshot capture instructions (user runs these)

Task 1 has verified that the Plan 27-02 compliance footer is present in the codebase and that the four existing screenshots are the expected 1408 x 3040 dimensions. You now need to capture four new screenshots and save them to the exact paths below, overwriting the existing files.

### Preparation (one-time)

1. **Boot an iPhone 16 Pro Max simulator** (or connect your physical device):
   ```
   xcrun simctl list devices available | grep "iPhone 16 Pro Max"
   xcrun simctl boot "<DEVICE UDID from above>"
   open -a Simulator
   ```

2. **Build and run Kindred on the simulator** from Xcode (⌘R). Wait for the feed to load so you see real Spoonacular recipe cards.

3. **Optionally fake a reviewer-friendly status bar:**
   ```
   xcrun simctl status_bar "<DEVICE UDID>" override --time "9:41" --batteryState charged --batteryLevel 100 --wifiBars 3 --cellularMode active --cellularBars 4
   ```

### English screenshots (en-US)

4. **Set the simulator language to English** (Settings app → General → Language & Region → iPhone Language → English (US)). Restart Kindred after the language change.

5. **Capture `02-recipe-feed.png` (en-US):**
   - Navigate to the main feed tab
   - Scroll so the "Popular Recipes" heading is near the top of the viewport
   - Ensure multiple recipe cards are visible
   - Ensure no modal / sheet / banner ad is covering the content
   - Capture:
     ```
     xcrun simctl io booted screenshot Kindred/fastlane/screenshots/en-US/02-recipe-feed.png
     ```
   - Verify: `sips -g pixelWidth -g pixelHeight Kindred/fastlane/screenshots/en-US/02-recipe-feed.png` → must show `1408 x 3040`

6. **Capture `05-recipe-detail.png` (en-US):**
   - Tap any recipe card to open the detail view
   - Scroll DOWN past the hero image, past ingredients, past instructions, until you see the compliance footer near the bottom
   - The footer must show BOTH lines:
     - "Nutrition estimates from Spoonacular. Not for medical use."
     - "Powered by Spoonacular →" (link with arrow icon)
   - Ensure the footer is fully visible in frame (not cut off at the bottom)
   - Capture:
     ```
     xcrun simctl io booted screenshot Kindred/fastlane/screenshots/en-US/05-recipe-detail.png
     ```
   - Verify dimensions are 1408 x 3040

### Turkish screenshots (tr)

7. **Switch simulator language to Turkish** (Settings app → General → Language & Region → iPhone Language → Türkçe). Restart Kindred.

8. **Capture `02-recipe-feed.png` (tr):**
   - Navigate to the main feed tab
   - IMPORTANT: the "Popular Recipes" heading is a hardcoded English literal in FeedView.swift line 142. It will render as "Popular Recipes" in Turkish mode too. This is a known current-state constraint acknowledged in this plan; capture what the build actually renders. Do NOT edit the Swift code — feed heading localization is out of scope for Phase 27.
   - Scroll so the heading is near the top
   - Capture:
     ```
     xcrun simctl io booted screenshot Kindred/fastlane/screenshots/tr/02-recipe-feed.png
     ```
   - Verify dimensions are 1408 x 3040

9. **Capture `05-recipe-detail.png` (tr):**
   - Tap any recipe card to open the detail view
   - Scroll DOWN to the compliance footer
   - The footer MUST show BOTH lines in Turkish:
     - "Besin değerleri Spoonacular tarafından sağlanır. Tıbbi tavsiye için kullanılmaz."
     - "Spoonacular tarafından desteklenmektedir →"
   - If Turkish text does NOT render, confirm the simulator language actually switched and that the app was fully restarted (kill from app switcher). If it still shows English, STOP and reply with `blocked: turkish footer not rendering` — this is a Plan 27-02 bug that needs a gap-closure plan.
   - Capture:
     ```
     xcrun simctl io booted screenshot Kindred/fastlane/screenshots/tr/05-recipe-detail.png
     ```
   - Verify dimensions are 1408 x 3040

### Final checks before resuming

10. **Confirm file modification timestamps are newer than the baseline:**
    ```
    BASELINE=$(cat /tmp/27-04-baseline-epoch.txt)
    for f in Kindred/fastlane/screenshots/en-US/02-recipe-feed.png Kindred/fastlane/screenshots/en-US/05-recipe-detail.png Kindred/fastlane/screenshots/tr/02-recipe-feed.png Kindred/fastlane/screenshots/tr/05-recipe-detail.png; do
      MTIME=$(stat -f '%m' "$f")
      if [ "$MTIME" -gt "$BASELINE" ]; then
        echo "OK: $f updated"
      else
        echo "STALE: $f — recapture this file"
      fi
    done
    ```
    All four must report "OK".

11. **Visually open each file in Preview.app** and confirm:
    - `02-recipe-feed.png` (both locales): "Popular Recipes" heading visible, multiple cards visible
    - `05-recipe-detail.png` (en-US): English disclaimer + "Powered by Spoonacular →" visible
    - `05-recipe-detail.png` (tr): Turkish disclaimer "Besin değerleri Spoonacular..." + "Spoonacular tarafından desteklenmektedir →" visible

12. **Clear status bar override** (if used):
    ```
    xcrun simctl status_bar "<DEVICE UDID>" clear
    ```

### How to resume

Reply "approved" with a brief note confirming:
- All four files were overwritten at the exact paths above
- All four files are 1408 x 3040
- Detail screenshots show the compliance footer in both locales
- (Optional) Any deviations or issues noticed during capture

If any capture step fails, reply with a specific error message:
- `blocked: turkish footer not rendering` — if the Turkish disclaimer didn't appear on the detail screen (Plan 27-02 bug)
- `blocked: wrong dimensions` — if sips reports dimensions other than 1408 x 3040
- `blocked: popular recipes heading missing` — if the feed shows "Viral near you" or similar stale UI (Plan 27-02 / earlier migration bug)
  </action>
  <verify>
    <automated>test "$(sips -g pixelWidth Kindred/fastlane/screenshots/en-US/02-recipe-feed.png 2>/dev/null | awk '/pixelWidth/ {print $2}')" = "1408" && test "$(sips -g pixelWidth Kindred/fastlane/screenshots/en-US/05-recipe-detail.png 2>/dev/null | awk '/pixelWidth/ {print $2}')" = "1408" && test "$(sips -g pixelWidth Kindred/fastlane/screenshots/tr/02-recipe-feed.png 2>/dev/null | awk '/pixelWidth/ {print $2}')" = "1408" && test "$(sips -g pixelWidth Kindred/fastlane/screenshots/tr/05-recipe-detail.png 2>/dev/null | awk '/pixelWidth/ {print $2}')" = "1408" && BASELINE=$(cat /tmp/27-04-baseline-epoch.txt) && test "$(stat -f '%m' Kindred/fastlane/screenshots/en-US/05-recipe-detail.png)" -gt "$BASELINE"</automated>
  </verify>
  <done>
- Human replied "approved" on the checkpoint
- All four target files exist at 1408 x 3040
- At least the en-US detail file mtime is newer than the baseline epoch (smoke test; Task 3 verifies all four)
- Human confirmed visual inspection of the footer in both locale detail screenshots
  </done>
</task>

<task type="auto">
  <name>Task 3: Post-capture verification and cleanup</name>
  <files>.planning/phases/27-app-store-compliance/27-04-SUMMARY.md</files>
  <action>
After the human checkpoint resumes with "approved", run automated verification on the four replacement files to catch accidental stale files, wrong dimensions, or empty PNGs.

**Step 1 — Dimension check (all four files):**
```bash
for f in Kindred/fastlane/screenshots/en-US/02-recipe-feed.png Kindred/fastlane/screenshots/en-US/05-recipe-detail.png Kindred/fastlane/screenshots/tr/02-recipe-feed.png Kindred/fastlane/screenshots/tr/05-recipe-detail.png; do
  W=$(sips -g pixelWidth "$f" | awk '/pixelWidth/ {print $2}')
  H=$(sips -g pixelHeight "$f" | awk '/pixelHeight/ {print $2}')
  echo "$f: ${W}x${H}"
  [ "$W" = "1408" ] && [ "$H" = "3040" ] || { echo "FAIL: $f dimensions ${W}x${H} (expected 1408x3040)"; exit 1; }
done
```
All four must print `1408x3040`. If any fail, the screenshot is the wrong resolution — report to the human and re-open Task 2 for the specific file.

**Step 2 — Timestamp freshness check:**
```bash
BASELINE=$(cat /tmp/27-04-baseline-epoch.txt)
for f in Kindred/fastlane/screenshots/en-US/02-recipe-feed.png Kindred/fastlane/screenshots/en-US/05-recipe-detail.png Kindred/fastlane/screenshots/tr/02-recipe-feed.png Kindred/fastlane/screenshots/tr/05-recipe-detail.png; do
  MTIME=$(stat -f '%m' "$f")
  [ "$MTIME" -gt "$BASELINE" ] || { echo "FAIL: $f is older than baseline ($MTIME <= $BASELINE) — file not updated"; exit 1; }
done
```
All four mtimes must be newer than the baseline epoch recorded in Task 1. If any are stale, the human may have captured only some files — re-open Task 2 for the missing ones.

**Step 3 — File size sanity:**
```bash
for f in Kindred/fastlane/screenshots/en-US/02-recipe-feed.png Kindred/fastlane/screenshots/en-US/05-recipe-detail.png Kindred/fastlane/screenshots/tr/02-recipe-feed.png Kindred/fastlane/screenshots/tr/05-recipe-detail.png; do
  SIZE=$(stat -f '%z' "$f")
  [ "$SIZE" -gt 100000 ] || { echo "FAIL: $f is only $SIZE bytes — likely empty or corrupt"; exit 1; }
done
```
Real App Store screenshots at 1408x3040 are typically 1-5 MB; anything under 100 KB suggests a corrupt or blank file.

**Step 4 — Cleanup:**
```bash
rm -f Kindred/fastlane/screenshots/en-US/.staging.txt Kindred/fastlane/screenshots/tr/.staging.txt /tmp/27-04-baseline-epoch.txt
```

**Step 5 — Write `27-04-SUMMARY.md`** including:
1. Each captured file's final dimensions and byte size
2. Timestamp deltas showing files are newer than baseline
3. Confirmation that Plan 27-02 footer rendered correctly in the detail screenshots (from the human's "approved" note)
4. Any known issues (e.g., Turkish feed heading showing English — expected per FeedView.swift hardcoded literal)
5. Next step: Phase 28 `fastlane deliver` will ingest these files automatically

**DO NOT:**
- Re-modify the PNG files.
- Run `fastlane deliver` or any submission command (Phase 28 scope).
- Delete the `.staging.txt` files before running the dimension/timestamp/size checks — they are evidence if something went wrong.
  </action>
  <verify>
    <automated>test "$(sips -g pixelWidth Kindred/fastlane/screenshots/en-US/02-recipe-feed.png | awk '/pixelWidth/ {print $2}')" = "1408" && test "$(sips -g pixelHeight Kindred/fastlane/screenshots/en-US/02-recipe-feed.png | awk '/pixelHeight/ {print $2}')" = "3040" && test "$(sips -g pixelWidth Kindred/fastlane/screenshots/en-US/05-recipe-detail.png | awk '/pixelWidth/ {print $2}')" = "1408" && test "$(sips -g pixelWidth Kindred/fastlane/screenshots/tr/02-recipe-feed.png | awk '/pixelWidth/ {print $2}')" = "1408" && test "$(sips -g pixelWidth Kindred/fastlane/screenshots/tr/05-recipe-detail.png | awk '/pixelWidth/ {print $2}')" = "1408" && test -f .planning/phases/27-app-store-compliance/27-04-SUMMARY.md && ! test -f Kindred/fastlane/screenshots/en-US/.staging.txt && ! test -f Kindred/fastlane/screenshots/tr/.staging.txt</automated>
  </verify>
  <done>
- All four screenshots are exactly 1408 x 3040 pixels
- All four file mtimes are newer than the baseline captured in Task 1
- All four files are > 100 KB (not empty or corrupt)
- Staging breadcrumb files removed
- `27-04-SUMMARY.md` exists with dimensions, timestamps, byte sizes, and human approval quote
  </done>
</task>

</tasks>

<verification>
**Automated (post-checkpoint):**
- `sips -g pixelWidth -g pixelHeight` reports exactly 1408 x 3040 for all four files
- `stat -f '%m'` shows mtimes newer than the baseline epoch
- `stat -f '%z'` shows all four files are > 100 KB
- Staging breadcrumb files removed
- `27-04-SUMMARY.md` exists

**Human (covered by the checkpoint):**
- User visually confirms "Popular Recipes" heading visible in feed screenshots (both locales render as English due to hardcoded literal — known and acceptable)
- User visually confirms Spoonacular attribution footer in both detail screenshots
- User visually confirms Turkish disclaimer text in the tr detail screenshot
- User confirms approval via "approved" message on the checkpoint

**Phase-level gap (to surface in SUMMARY.md):**
FeedView.swift line 142 uses a hardcoded `Text("Popular Recipes")` literal that is NOT routed through `String(localized:, bundle:)`. As a consequence the `tr` feed screenshot will show "Popular Recipes" instead of "Popüler Tarifler". This is a known current-state constraint acknowledged in the ROADMAP success criterion text (which also says "Popular Recipes"). Localizing the feed heading is a follow-up polish task, not blocking STORE-03.
</verification>

<success_criteria>
- All four screenshots replaced at `Kindred/fastlane/screenshots/{en-US,tr}/{02-recipe-feed,05-recipe-detail}.png`
- All four are 1408 x 3040 PNG files
- Detail screenshots in both locales show the Plan 27-02 compliance footer (disclaimer + attribution link) clearly visible in frame
- Turkish detail screenshot shows the Turkish-localized disclaimer
- Feed screenshots show the "Popular Recipes" heading (as rendered by the current build)
- File mtimes are newer than the start of Plan 27-04
- Human has acknowledged visual review via the checkpoint resume signal
- `27-04-SUMMARY.md` documents dimensions, timestamps, byte sizes, and the known hardcoded-heading caveat
</success_criteria>

<output>
After completion, create `.planning/phases/27-app-store-compliance/27-04-SUMMARY.md` with:
1. Dimensions + byte size + mtime for each of the four files
2. Quote from the human's "approved" checkpoint message
3. Visual confirmation checklist (disclaimer text, attribution link, Turkish rendering, Popular Recipes heading)
4. Known caveat: feed heading shows "Popular Recipes" in tr locale due to hardcoded literal in FeedView.swift:142 (out of Phase 27 scope)
5. Hand-off to Phase 28: `fastlane deliver` will ingest these files automatically during the release lane
</output>
